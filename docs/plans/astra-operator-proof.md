# Astra operator proof

**Direct development resumed on 8 September.** The operator requested continuing
without the blocked Star Map destination. That destination is now verified
Stopped. Astra reopened installed 0.1.80 and restored its compiled browser
surface; all three installed health/conversation/connection checks pass. Actual
browser conversation loading and automatic local access pass. Floe recovered the
interrupted image-service Builder from retained work and returned named saved
evidence; the original operator request was not resent. Independent verification
found **F83: the exact authored package is rejected by Floe's isolated realm**.
Its Node tests do not establish Floe compatibility, and Extension delivery is
incomplete. No package was installed or approved. See
[resume evidence](resume-without-star-map-20260908.md) and
[Extension host attempt](extension-host-attempt-20260908.md).

Latest installed correction: **0.1.80, F82**. Floe now reads recent Context
contributions first through existing Bus pagination. The actual handoff returned
in 17.518 seconds using 26,014 reported tokens, three responses and one history
call. Its new controls open the original recorded decision and the exact Gallery,
Test report and Build report. All saved work, settings and accounts survive;
desktop health, nine live connections and eight saved-conversation Workspaces
pass. The prior handoff is now in the Context: the observed 90.25% token saving
is not a controlled attribution or general efficiency proof. Continue the
accepted journey; no full gate is closed and native window use remains unproven.
See [recent Context history](recent-history-20260907.md).

Earlier installed correction: **0.1.79, F81**. The actual Floe Actor returned a
named original-approval reference and named exact Gallery, Test report and Build
report attachments. The browser opens the saved decision directly in one click,
runs the Gallery, and opens both reports. The original reference still works after
installed restart and reload. All 1,588 earlier Events, saved work, decisions,
Actor heads, accounts, settings and bindings survive. Desktop health, all nine
live connections and all eight saved-conversation Workspaces pass. The handoff
used 266,729 reported tokens across eleven responses; six forward history pages
exposed F82. The Bus already supported recent-history reads but the model tool
did not expose them at that checkpoint. Full gates and native window interaction remain unproven.
See [conversation references](conversation-references-20260907.md).

Earlier installed correction: **0.1.77, F80**. A pending approval can explicitly
deliver its resolved decision to an existing conversation participant. Resuming
the same approved operation preserves its original intent and rechecks current
authority. Schema 14 repairs the existing-data upgrade omission caught in 0.1.76.
The live upgrade preserves all previous records, Actor heads, accounts, settings,
grants and bindings. All nine desktop live connections and saved reads in all
eight Workspaces with conversations pass. The real Floe Actor configured its
response on the original request. Astra operated the local test approval through
the browser; Floe resumed and exported the reviewed Gallery under its own
authority, then returned the verified result in 31.469 seconds without another
message. Its receipt was used once; the original action, earlier rejected clone,
eight reviews, three saved versions and campaign Actor heads remain unchanged.
The Gallery opens and runs in the browser. Continuation used 35,848 reported
tokens over five responses; setup used 399,119 over fourteen. The pending request
and final result needed clearer navigation at that checkpoint; F81 now proves the
conversation handoff. No full gate closes.
See [approval response](approval-response-20260907.md).

Earlier installed correction: **0.1.75, F78/F79**. A real approval attempt created
a valid request, then Floe cloned it with a changed action. The browser recorded
**Changes requested** on the clone. Corrected instructions preserve the original
action; the client now keeps recorded decisions, reasons and named evidence in
the conversation. Browser reload, reopening that request and opening its exact
test report pass. Forty-eight client tests and the build pass. Upgrade retained
all 1,577 earlier Events, 36 Actors, 31 versions, 11 Scope executions, both
requests, accounts, configuration, grants and bindings. The fresh real-model
attempt preserves the original request and attaches named evidence, but confirms
that automatic continuation is unavailable for this unbound request (F80).
Its 15 responses cost 496,845 reported tokens; the full outcome is incomplete.
At that checkpoint no approval or local export had occurred. The 0.1.77 proof
above now completes that local decision and automatic continuation.
See [approval feedback](approval-feedback-20260907.md).

The **0.1.74, F77** correction also passes on installed 0.1.75. The persistent desktop
“Reconnecting” label came from a refused installation health read after a healthy
live connection. A fixed native health method now reads the existing projection
without widening generic transport authority. All nine installed Workspace
streams pass; all eight Workspaces with conversations pass saved reads; runtime
health reports connected. The browser reconnects automatically after restart.
Saved work, accounts, settings and bindings passed preservation checks.
See [desktop health proof](desktop-runtime-health-20260907.md).

The build contains the tested exact-version export mechanism and corrected
approval instructions earned by **F75/F76**. The actual model/app repeat created
the requests described above; the 0.1.77 repeat now proves the local decision and
automatic continuation.
See [review decision attempt](review-decision-20260907.md). Native window testing
and the complete product gates remain open.

Status: Astra owns implementation. On the operator's instruction, proving now
uses the installed Floe and a new test Workspace, reusing its existing account.
The first real model turn completed and saved a brief. The installed app is now
0.1.80. The real model completed a precise single-phrase correction, attached the
new retained version to its reply, and left the previous saved version readable
through version history. The browser verified both versions and the clearer reading
view. Complete per-response accounting and bounded discovery (F23) are installed;
the measured repeat used 134,729 tokens versus 240,335, with correctness preserved.
Direct-response Stop and deliberate continuation after restart now pass in the
installed app (F18). Floe has now built and retained the browser game. F26 exposed
the missing interactive preview. The exact saved game now opens in a larger
preview, with keyboard/pointer play, timeout, Play Again, Stop and Escape proof.
Floe also produced a 3D scene with three independent objects. F27 is repaired:
the original scene and objects now open and rotate in the installed app's browser
client. A selective lantern correction exposed F29: an unrelated rejected watcher
call disabled the shared Bridge client. The correction files are preserved;
the connection repair is installed. F30's startup import conflict is repaired:
the installed restart retained the saved Actor settings and resumed the waiting
message without resending it. Selective correction now passes: the teal lantern
and revised scene are retained as version 2, with original versions, other
objects, geometry, positions and glow preserved. F28 now passes: the model
reattached the exact saved versions with named, working Open buttons and no file
or version changes. F31 records excessive discovery
and recovery cost for the successful handoff, requiring measured diagnosis.
F32's missing runtime-binding notification is repaired and installed. Continuing
the actual independent review now delivers work to the existing reviewer, but
F33 exposed copied permission references belonging to Floe. Explicit bounded
delegation and early publication validation are now installed. An existing-data
startup fault in 0.1.47 was reproduced and corrected by the schema 13 upgrade.
The retained accounts, configuration, grants, history and saved work pass
preservation checks. The existing collaborator has now completed the independent
review, and Floe returned a named saved result which opens in the actual browser.
This proves repair and reuse of an existing collaborator. In 0.1.49, Floe also
created a new collaborator after startup, configured its access/model, received
its independent review and published a new named result. Creation required
autonomous recovery from F37. Installed 0.1.50 now proves stopping an active
collaborator from the originating conversation and retaining cancellation after
restart (F38). Deliberate continuation passes on 0.1.54. Installed 0.1.55 now
proves nested returned-work controls, cancellation and restart retention.
F41 gives the conversation list current response labels. The campaign Workspace
was created through the real local browser on 0.1.56 (F42). Correcting carried
conversation state restored the new Workspace's model chooser (F43); its existing
ChatGPT account and GPT-5.6 Sol/high selection now show Ready. That client
correction is installed in 0.1.57. The first campaign attempt failed on oversized
image input and exposed missing Scope creation in the shared operation contract
(F44/F45). Its files and original request are retained for continuation.
Installed 0.1.52 closes F39's startup history replay: the browser opens the
conversation list and stopped review without replaying old activity or repairing
account access. Native UI startup and wider responsiveness still need proof.
The fresh-model repeat now proves F37's first publication without the earlier
workaround. Installed 0.1.53 also proves exact saved inputs on an Actor request
(F40) and complete paged reading of the saved game (F34).
The full acceptance campaign remains
in progress; this is a completed proving increment, not full product completion.

### Automatic named return, bounded history and validation — F71, F73, F74

Installed 0.1.73 preserves valid history pages and server-issued cursors, and
refuses unavailable fixed Contexts before publishing or rolling back a plan.
The 65 focused regression/routing/isolation tests pass. The native broker rejects
retained bad revision 10 and accepts corrected revision 11. All eight Workspaces
with saved conversations pass the installed desktop read check; 1,562 earlier
Events, account files, configuration, Actor heads and bindings survived upgrade.

The identical fresh review now completes with an accurate saved 22/22 verdict
and automatically returns **Gallery**, **Test report** and **Build report** to
the original conversation. All three open in the actual browser, including the
running gallery preview. No duplicate review request, timed check or intervention
was needed during this attempt. Seven earlier runs and all exact versions remain
unchanged. Astra prepared the corrected return configuration beforehand; this
proves its unaided operation, not autonomous authorship of that repair.

The repeat used 28 responses and 595,342 reported tokens over 180.213 seconds,
54.84% fewer tokens than the earlier successful same-model/evidence review.
This is one controlled repeat; earlier recovery costs remain recorded below.
Live history retrieval stayed within its size budget with no failed history calls.
See [history, validation and return proof](history-and-plan-validation-20260907.md).
The broader goal, native window interaction and full acceptance journey remain
unproven. Next, attempt the exact-version review decision in the operator surface.

### Completion return and discoverable instructions — F71, F72

Installed 0.1.72 describes and validates the existing placement instruction
binding. The first automatic return reached Floe but omitted the placement's
instructions because they had been stored in an inert configuration field.
After the discovery correction, Floe repaired its saved return step through
normal operations. The identical fresh review now delivers the verdict through
its stored Edge and back to the original conversation without another request
or timed check. The saved verdict correctly records 22/22 checks; all earlier
runs and exact versions are preserved.

The result initially used generic attachment button labels, a recurrence of
F68. Floe resent named attachments without repeating the review; the browser
opens the gallery and both reports. Its saved return change also selected an
incompatible fixed Context, which validation incorrectly accepted (F74). Astra
restored the proven Context policy and explicit named communication through
normal operations. Only that placement changed; all seven runs remain intact.

The automatic return used 26 responses and 767,856 reported tokens; its label
repair added 22 responses and 747,175 tokens. A same-quality efficiency gain is
not established. F73 records malformed/truncated history pages and failed cursor
attempts. The subsequent 0.1.73 proof above corrects F73 and F74 and repeats the
full named automatic return using corrected revision 11. Developer configuration
repair remains separate from unaided Floe operation.
See [completion-return evidence](completion-return-20260907.md).

0.1.72 packaging, focused contract tests and retained-state checks pass. All
eight Workspaces with saved conversations pass the installed desktop broker
read check. Native window interaction and the wider acceptance journey remain
unproven. No full end-state gate is closed by this bounded proof.

### Saved review evidence and return — F70, F71

Installed 0.1.71 lets both the operator and model read saved output Events through
the existing execution inspection. The browser's generic Actions surface exposes
the option and displays the exact verdict. Focused tests pass (25), the installed
build matches, retained state passes preservation checks, and the desktop broker
reads conversations in all eight Workspaces with saved conversations.

The identical fresh review completes with an independently verified saved count
of 22 total, 22 passed and zero failed. Floe returns the named gallery and both
reports; all three open in the compiled browser. The reviewer receives the work
once, with no duplicate direct request or intervention during the attempt. The
same model/evidence outcome uses 1,318,226 reported tokens across 49 responses,
41.6% fewer than the previous attempt's 2,257,948 across 91. Earlier runs and
exact versions remain unchanged; no external publication occurs.

**F71 in the 0.1.71 attempt:** Floe collects the result through two timed checks. The first
misses publication by 2.4 seconds and emits stale active status. Next, correct
coordination instructions to compose a stored output return to Floe. The 0.1.72
repeat above now proves the return to the originating Context without status
pulses. The subsequent named-button recovery and successful 0.1.73 repeat are
recorded separately. See
[saved output handoff proof](output-evidence-handoff-20260907.md).

### Desktop exit and fresh review — F69, F70

Installed 0.1.70 fixes a reproduced crash when new work logs to console pipes
whose desktop reader has exited (F69). A real browser question now starts and
finishes with the desktop absent. Reopening the desktop retains the same service.
The upgrade preserves 1,531 earlier Events, accounts, grants and all 36 Actor
heads; the desktop broker conversation check passes in eight Workspaces.
The fresh review also proves the updated F67 instructions complete a new run,
but its handoff repeats work and the saved verdict miscounts the test report
(F70). The later 0.1.71 repeat above records their correction and the remaining
timed-collection defect. See
[desktop continuity proof](desktop-host-continuity-20260907.md).

### Fresh campaign review — F55, F67, F68

The real Scope reviewer now receives and publishes against its current work on
the first attempt, supplying F55's missing live repeat. Its saved verdict passed
the 22 retained checks. F67 exposed confusion between completing the review and
approving later publication. The existing reviewer has now completed the same
run from its saved verdict, and Floe clarified its instructions and returned
three named outputs. F68 corrects raw run labels, stale completion status and
three identically named evidence cards. The real browser opened the gallery
and both reports. Installed 0.1.69 retains the completed review, all 1,517 earlier
Events, accounts and 36 Actor settings. The desktop conversation-read repeat
passes in all eight Workspaces with conversations. See [current review proof](campaign-review-completion-20260907.md) for exact
records, costs, recovery observations, and remaining verification.

### Desktop conversation loading — 0.1.68

**F66:** conversations failed across the desktop Workspaces because the native
broker rejected the existing conversation read routes. The browser did not use
that broker path, so earlier browser checks missed the regression. The correction
and repeatable live desktop request check are recorded in
[conversation loading proof](conversation-loading-fix-20260906.md).

### Pipeline visual revision — 0.1.65

The presentation has been restored and tested against a controlled canonical image pipeline. See [revision evidence and remaining success criteria](pipeline-visual-revision-20260906.md). This supersedes the pre-revision visual status below; P6 remains partial. F64 and F65 record the observed identity/addressing gaps exposed by the attempt. Normal operator setup, assigned output and selected-step notes subsequently pass in the [app participation revision](actor-participation-revision-20260906.md), which also corrects F57's CLI Workspace selection failure.

### Approved pipeline visual fidelity — 2026-09-06

The operator observed that pipeline and Work views no longer resembled the
approved prototypes. The original **Simplify floe-app operator entry** task
contains explicit approval on 2026-09-02 of `pipeline-focus-prototype.html`.
The original file was recovered and its source, Registry and Contact sheets
states exercised in the browser. The operator then explicitly required a visual
match in the next revision. The unchanged prototype and acceptance criteria are
now preserved at `docs/design/approved-pipeline-focus/`; PRODUCT.md records the
requirement and end-state gate P6 remains pending.

- **F61 — Cards lost visual detail.** The prototype groups actor identity,
  images, named inputs, outputs and conversation access in rich cards. Current
  neighbouring cards contain labels/status and sometimes Context IDs; focused
  execution details expose technical evidence separately. The approved card
  presentation has not been preserved.
- **F62 — Branch connections lost clarity.** The prototype connects its
  selected output to individual downstream cards with image previews. Current
  presentation uses one CSS arrow between layers and compact downstream cards.
  Progressive navigation survives, but visual fidelity does not.
- **F63 — Layout stacks earlier.** The current app changes to one column below
  1120px viewport width; the prototype does so below 800px. The real browser at
  922px showed a vertically stacked pipeline. App navigation also consumes
  width, so comparison must record both viewport and available content width.

The selected **Pipeline view test** Workspace displayed **Concept reference
pipeline**, plan revision 1, **Plan only**, with no recorded run. Missing outputs
in that state are not themselves proof of an output-rendering defect. The
prototype contains saved example artefacts and explicitly proposed Context
branches; those must not be fabricated in live work. ADR-0010's canonical
connections remain authoritative while the approved appearance is restored.

No visual restoration has shipped at this checkpoint. P6 is a required check for
the next revision, not a completed UI change. The full end-state remains open.

### App capability parity assessment — 2026-09-06

**F59 — The app exposes selected actions, without the accepted general action
fallback.** The operator asked how far `floe-app` is from being a complete
surface for the same capabilities available to model-backed Actors. The need is
direct, understandable access to every permitted semantic action, including
unfamiliar actions. Asking the Floe actor to perform an action does not establish
direct human-actor control under the human actor's own authority.

Authenticated discovery against installed 0.1.63 returned 140 Workspace
operation descriptors across 18 categories. A scan of non-test app source found
explicit string references to 26 of those operation IDs. This is a source
coverage clue, **not a completion percentage**: helpers are not necessarily
reachable product controls, existing specialised transports may use other
references, availability can depend on a selected target and grants, and a
descriptor does not prove successful execution. Host actions, runtime-local
tools, Extension-contributed actions, and the complete cross-client matrix are
outside this count. Snapshot:
`C:/Development/_temp/floe-upgrade-proof-20260904/app-capability-coverage-0.1.63.json`.

Initial source inspection found no general descriptor-driven action form and no direct
app use of approval decisions, permission delegation, Extension lifecycle,
Connector lifecycle, Scope plan operations, or execution retry/redo/pause/resume.
Existing app paths cover conversation, selected Actor/model settings, retained
Artefact inspection and execution Stop. Some older developer surfaces still use
specialised endpoints. Shared transport is present; complete app coverage and
equivalent authority, validation, consequences and audit remain unproven.

This is principally a client/representation gap. ADR-0011 already requires a
schema-driven action fallback, rich presentation where needed, and the same
canonical handler. The next app milestone should deliver that accepted contract
through actual operator tasks: start with the campaign's missing approval
interaction, expose relevant available actions over selected canonical records,
retain required confirmations and receipts, and prove cancellation/recovery and
newly discovered actions. Do not make the operator copy IDs or author raw JSON
to achieve ordinary work. Do not invent a new substrate primitive or an
independent app permission model. Full end-state gates remain required.

**First F59 implementation — installed 0.1.64.** The normal Workspace header
now opens **Actions**, which discovers the live catalogue and renders input
fields from the selected descriptor. **Actions for this work** carries the exact
selected ScopeExecution reference. New supported descriptors do not require
another hardcoded app handler. Read results and refusals are distinct; writes
show the selected choices before dispatch and retain any required trusted
confirmation. Fresh discovery rechecks authority and changed contracts before
dispatch. A submitted request and its idempotency key are retained for the
browser session, allowing the exact intent to be retried after a lost response
and remount. A later refusal does not erase an earlier unknown outcome.
Asynchronous acceptance is not shown as completion; receipt retrieval is explicit.

Approval results have a compact presentation of effect, reason, status,
decision progress and exact saved evidence. Approval result schema version 2
adds canonical `resource_ref` values derived from the existing request and
receipt revisions. This is a read projection, not a new stored resource or app
authority model. The app can select those references without deriving identity
or mutation revisions from arbitrary field names.

The real browser opened the catalogue, searched for approval, and invoked
`approval.list` directly. Receipt `opinv_15605ea9cd9f6c3d47b6cd8a636b60cf`
records the ordinary local operator principal, successful completion, zero
matching requests and no changed resource references. No model Delivery was
created; all three campaign version references and content hashes remain intact.
This initial browser check used the new source client with installed 0.1.63.
The completed 0.1.64 Windows build returned exit zero and both installers.
Installation retained all 1,478 earlier Events, 33 Actor heads, runtime bindings,
existing grants, account files and configuration, with integrity and foreign-key
checks passing. Recovery:
`C:/Users/jfenech/.floe/recovery/before-0.1.64-20260906-final`.
Installed service SHA-256:
`6ad740ef52ae968c1fb8e38d1e4202dd4e8754cfd62e672531aefae6ba516a86`.

The browser then reopened without account repair and repeated approval inspection.
Receipt `opinv_07b9c45c6135ffd133d23a9857fe033c` confirms result schema version 2,
zero requests, the same ordinary operator principal and no changed references.
From the actual completed campaign run, **Actions for this work** opened its
available operations without copied IDs. `scope.execution.inspect` receipt
`opinv_dbc1f55529dc7e6f4fea02077bf413c7` returned the correct completed execution
and its exact canonical revision. Following the reviewer's returned Node
reference exposed its output, retry and redo actions. No mutation was submitted.
The generic inspection result remains verbose and its reference labels need
further product work. No model Delivery was created by these app inspections;
all three campaign versions and hashes remain unchanged. Saved evidence:
`app-actions-0.1.64-final.json`, `delegation-upgrade-0.1.64.json`,
`build-0.1.64-final.log`, and `build-0.1.64-exit.json` in the proof directory.
This is source-served browser proof against the installed service; packaged
native interaction, standalone web and mobile proof remain pending.

Fifteen focused app checks pass, covering typed optional/union/open-object
inputs, unknown discovered actions, lost-response/remount replay, permissions,
changed effects, confirmation cancellation, canonical approval selection,
asynchronous receipts and the existing Work controls. Fifty approval and
documentation checks pass, including list/inspection references and the retained
decision contract. App and Bus typechecks pass. Logs are
`app-actions-final-tests.log` and `approval-references-tests.log` under the proof
directory. The broader transport checks passed earlier in this increment.

**F60 — Review readiness has no actionable approval request.** Both authenticated
inspection and the new app surface confirm that the campaign has no canonical
approval request. Its completed review and message saying approval is needed
are not an approval decision surface. A real request still needs the exact
intended publishing action and destination. No approval or external publication
was invented for this test. Full approval decision, resource-picker coverage,
all 140 actions, host/runtime-local tools, Extension-contributed actions,
native/mobile operation and session recovery across application restart remain
unproven. F59 is an implementation increment, not complete capability parity.

**Review handoff legibility repeat.** The normal recovery conversation received
request `evt_98708c86-4bf0-4022-b284-db835976e7c5`, asking for the exact reviewed
gallery and test evidence with clear openable names, without changing saved work
or rerunning the review. Delivery `del_cfaa5f13-1535-4efe-9c82-4e8438dc7385`
completed with no semantic refusals. The browser opened the named gallery,
ran its retained HTML preview, returned through Stop preview, and opened the
named test report. That report contains the same site hash, 22 recorded passing
checks, and the retained licensing/accessibility limitations. All three original
versions and content hashes remain unchanged. No new Scope run or version was
created. A third attachment still uses the generic label `Saved result 1` and
the report is displayed as raw JSON; requested references now work, but broader
legibility is incomplete.

The request used seven reported model responses and 104,762 total tokens,
including 35,584 cache-read tokens, with four history reads, three Artefact reads
and one emit. This is observed cost, not an efficiency improvement claim. Saved
proofs are `campaign-legibility-0.1.63-final.json` and
`campaign-legibility-preservation-0.1.63.json` under the same proof directory.
No active Delivery remained at the final preservation check. This proof uses the
source-served browser client backed by the installed service; packaged native,
standalone web and mobile proof remain required. No external publication or
publishing approval occurred.

### Campaign Workspace onboarding — 2026-09-05

**F42 — The local browser cannot register a Workspace.** The campaign journey
attempted to add **Floe campaign proof** at
`C:/Development/_temp/floe-campaign-proof-20260905` through the actual workspace
switcher. Installed 0.1.55 returned `Bus GET /v1/local/operations → 401` before
registration. The folder did not exist and no Workspace was created. An initial
automation path-escaping mistake was corrected before reproducing the refusal.

The need is to establish a Workspace from the already authorised local client,
including before any Workspace exists. This is a host/client adapter over the
existing canonical `workspace.register` operation, not a new primitive. The
browser was incorrectly calling the desktop host-token transport. The correction
uses an origin-bound local-cookie adapter with the same host-policy authority,
discovery, invocation, validation and receipts as the native host. Remote paired
sessions remain Workspace-bound, and browser content gains neither raw host
routes, credentials nor trusted confirmation.

The observed refusal was reproduced in tests before the correction. Afterwards,
38 Bus tests across six relevant files and 33 client tests pass (two existing
client todos remain). Tests include first/additional Workspace registration,
folder creation, idempotent replay, receipt retrieval, immediate use of the new
Workspace, caller-identity forgery and remote/disconnected/forwarded/foreign
request rejection. Bus TypeScript and nine documentation checks pass. Version
0.1.56 completed its Windows build and installation. The service SHA-256 is
`0636168b72063b01ed2f623a4e77283d7428ef4f7c5cccc75128b333c752d1a8`.
Recovery is `C:\Users\jfenech\.floe\recovery\before-0.1.56-20260905-final`.
All 1,410 earlier Events, grants, account files, configuration, 29 Actor heads
and runtime bindings survived; integrity and foreign-key checks pass. Evidence:
`delegation-upgrade-0.1.56.json`.

The actual browser then submitted the same path and name, confirmed folder
creation and opened **Floe campaign proof** at 09:20:23Z. Workspace:
`workspace_cde8e617-30c2-4638-9072-36df577e52f3`; completed host operation receipt:
`opinv_334401acbc3ad853e2a894726d318e41`. No account sign-in or pairing was needed.
Evidence: `campaign-workspace-ready-0.1.56.json` and the CUA registration sequence.

**F43 — Workspace changes carry conversation state across the boundary.** After
registration, the actual conversation list requested the new Workspace using
the previous Workspace's operator Actor and displayed 401. It later opened the
composer but its model chooser repeatedly reported no model connection despite
the new Floe Actor's retained unresolved binding. The need is to keep participant
authority, draft recipients and pending requests within their original
Workspace. This is client state ownership, not new authority or model setup.

The conversation component now resets its state as one unit when its Workspace
changes. The regression test first reproduced the wrong participant/Workspace
request, then proved the new participant and recipient are used, late old
results stay out of the new screen, and submission targets the new Workspace.
All 23 conversation tests pass. In the actual source-served browser against
installed 0.1.56, this correction restored the chooser. The operator selected
the existing ChatGPT account, GPT-5.6 Sol and high effort, then **Use model**.
The retained binding is resolved. Switching to **Floe operator proof** showed
the original conversations; switching back showed the campaign composer Ready
with its saved model.

Version 0.1.57 completed the Windows build/package and verified installation.
The installed service SHA-256 is
`b89a44f09b639c5149a4d7a0df85248b584afd13f0502041f7b28e39661364ea`.
Recovery is `C:\Users\jfenech\.floe\recovery\before-0.1.57-20260905-final`.
All 1,410 earlier Events, grants, account files, configuration, 31 Actor heads
and runtime bindings survived, with valid integrity and foreign keys. Evidence:
`delegation-upgrade-0.1.57.json`. After restart the real browser reopened the
campaign composer with the saved model Ready, without setup repair.

At 09:31:43Z the actual composer submitted the campaign outcome in this new
Workspace. It asks for Markdown cards to become tested gallery sites using an
external image service, exact-version approval before publication, selective
image correction, legible waiting/failure and continuation after interruption.
The first brief is a night market for independent makers. Event:
`evt_9f18d341-dd88-4344-86b7-897314084858`; Context:
`ctx_092a68ac-ea5f-4d22-a7f6-1b0c4fb55737`; Floe Delivery:
`del_4023fd02-89f5-4cda-9f91-5462f444da53`. The browser shows working status and
Stop; real model telemetry confirms capability discovery has started. The empty
Workspace baseline is `campaign-before-0.1.57.json`; the read-only observer is
`C:\Development\_temp\floe-campaign-proof-20260905.mjs`. This is a submitted
campaign attempt, not a completed website or full acceptance result.

The campaign attempt ended in a model-input refusal after the last image read at
09:37:38Z. Delivery `del_4023fd02-89f5-4cda-9f91-5462f444da53` is dead-lettered;
the original request is retained. There are 19 files, including the card, a site,
tests, candidate images and local scripts, but no published ArtefactVersions,
no collaborator team and no completed model reply. The browser displays the
provider error and tells the operator to check Settings, which does not resolve
the image-input problem. Evidence: `campaign-failed-0.1.57.json` and the actual
CUA failure view. Do not count this as a completed campaign or a native workflow.

**F44 — Scope creation is absent from the shared operation contract.** Floe
repeatedly searched for Scope creation and existing Scopes. Its granted catalogue
contains composition and execution operations, but no `scope.create` operation.
The current client-facing POST `/v1/workspaces/:workspace_id/scopes` calls
`store.createScope` directly; the canonical composition import requires an
already existing Scope. The attempt instead wrote local `tools/build.mjs`,
`tools/status.mjs` and `state/status.json` with its own `ready_for_review` state.
These files preserve evidence of the attempt; they are not an accepted second
work-state ledger. The smallest correction is to expose the existing Scope
creation mechanism through the same authenticated semantic contract used by
Actors and clients, then rerun the existing campaign attempt. No new primitive
or Scope model is needed. The source correction now adds `scope.create` and
`scope.list` to canonical discovery, makes both app creation and the existing
POST use the same handler, and includes the operations in new-Workspace Floe
policy. Existing saved Actor definitions are preserved. Fifty Scope/import Bus
tests and 28 Context/transport regression tests pass, as do 23 client tests
(two existing todos) and Bus TypeScript. The tests cover grants, Workspace
isolation, idempotent replay across both creation paths, identity refusals and
immediate composition in the created Scope. Installed 0.1.58 is verified below;
native campaign organisation still needs the real continuation to prove it.

**F45 — Image inspection can terminate the whole model turn.** The provider
rejected image input requiring 41,334 patches against a 30,000-patch limit.
`read_image` limits compressed file size to 20MB but forwards original image
bytes without a dimension or model-input bound. Candidate `textile2.jpg` is
9,802,734 bytes at 7,952 × 5,304 pixels, so the current byte guard does not prevent
this kind of refusal. Original image dimensions were read without modification;
evidence is `campaign-image-dimensions-0.1.57.json`.
The exact image contribution versus accumulated image context still needs
measurement; do not claim a particular pixel threshold from the error alone.
The correction belongs in bounded model image input and recoverable tool
feedback. Preserve the original image and work; do not ask the operator to
repair model settings or recreate the campaign. The source correction now
decodes supported images into orientation-aware previews bounded to 1,536 pixels
on either side, preserving original bytes and returning source SHA-256 and
dimensions. It retains the 20 MiB source limit, adds a 64-megapixel decode limit,
and returns recoverable tool errors for invalid images. Tests reproduce the
failed image's 7,952 × 5,304 dimensions and cover rotation, no enlargement,
corrupt input and unchanged originals. All 78 relevant Bridge tests and Bridge
TypeScript pass. The packaged sharp decoder and its dependency closure pass
PNG, JPEG, GIF and WebP checks using the shipped Node runtime in an isolated
folder without the checkout. Evidence: `image-sidecars-0.1.58.log`. This proves
the source and package. Installed 0.1.58 and the successful real image inspection
are recorded below; the full campaign remains in progress.

**F31 efficiency evidence:** This failed attempt reports 821,083 tokens across
22 model responses, with an agent-end coverage record for all 22. It made 37
capability-discovery calls (three failed), seven image reads, ten commands and
nine writes. The failed provider request does not establish zero additional
provider cost. There is no completed outcome for a savings comparison. Missing
capability access and unbounded image input must be corrected before using this
run as an efficiency baseline for successful work.

**F46 — Browser-delegated access is limited by the connection lifetime.** While
preparing the existing campaign Actor's grant for the two new Scope operations,
`issueWorkspaceOperationSession` was found to issue its source grants with the
same one-hour expiry as the browser's Workspace session. Delegation correctly
cannot outlive its source. This permits a bounded immediate retest, but it does
not prove durable permission management for continuing work. Do not silently
renew delegated access, widen every Actor's grants, replay configuration over
saved model settings, or count a temporary test grant as permanent provisioning.
Any temporary campaign grant must be removed from the Actor definition after
the retest so that its expiry does not leave an invalid saved grant reference.
Durable permission management remains an observed continuation requirement.

Version 0.1.58 completed the Windows build and verified installation. The
service SHA-256 is
`2296f04aa5c086b943b1bf02457d7b97c6e139a0c8402278b85d7ff9bb356bfa`.
Recovery is `C:\Users\jfenech\.floe\recovery\before-0.1.58-20260905-final`.
All 1,412 earlier Events, account files, configuration, grants, 31 Actor heads
and runtime bindings survived unchanged; integrity and foreign keys pass.
The installed image decoder and Command/Extension hosts also passed isolated
checks without the checkout. Evidence: `delegation-upgrade-0.1.58.json`.

Using the authenticated browser and canonical delegation/Actor publication,
only the campaign Floe Actor received the two new Scope operations for this
bounded retest. Its earlier grants and all other definition content were
preserved. Grant `capgrant_1d125f1e-9c20-4c0e-be41-70b8369dc1ec` expires at
11:33:33Z. Evidence: `campaign-scope-grant-0.1.58.json`. The actual browser
continued the original conversation at 10:34Z, retaining its request and files.
Delivery: `del_24d5e2fa-a72e-41a7-b408-aab3cc852d8c`. The previously rejected
image was read at 10:34:32Z as a 1,536 × 1,025 preview, and the model continued
through further image inspection and useful file work. Campaign completion,
native organisation and final token cost are still pending.

**F47 — Bounded discovery hides relevant operations behind incidental matches.**
The continuing model repeatedly searched short phrases. Repeating its queries
through the real browser on installed 0.1.58 showed `scope.create` at position
22 of 43 for `scope create`, outside the tool's first 20 results. `scope status`
placed `scope.execution.inspect` at position 23 of 29. Matching any query word
and returning registry insertion order made the relevant operations easy to
miss. Evidence: `discovery-query-0.1.58.json`.

The source correction ranks an exact operation identity first, then the number
of matched query words, then matches in the identity and title. It preserves
partial matches, existing filters, contracts, refusals and unqueried ordering.
Both interactive and unattended regression cases reproduced the failure before
the correction. Afterwards 18 discovery/operation tests across three files
pass, as do Bus TypeScript and nine documentation checks. Installed 0.1.59
repeats the actual queries: `scope create` now returns `scope.create` first;
`execution start` and `node output` also put their intended operation first.
`scope status` puts listing first and execution inspection within the first 20.
All previous matches remain. Evidence: `discovery-query-0.1.59.json`.

**F48 — Operation refusals hide the evidence needed for recovery from the model.**
The real campaign created `scope_night_market_campaign` at 10:43:46Z, proving
F44's installed creation path. Draft creation then failed three times: Floe
tried the Scope update timestamp, `1`, and an omitted revision. Each canonical
refusal contained the correct `current_revision: "none"`, but the Bridge put
that evidence only in tool `details`, which the runtime reserves for logs/UI.
The model received just the generic error sentence. It subsequently guessed an
unavailable `scope.inspect` operation. The retained refusal receipts are in
`campaign-continuation-0.1.58-observe.json`.

The correction includes the canonical refusal code, retryability, required
action, details and receipt identity in model-visible tool content for refused
receipts, rejected calls and invocation conflicts. It does not guess a retry or
change authority. Three regression cases reproduced the hidden evidence before
the correction; afterwards nine relevant Bridge tests and Bridge TypeScript
pass. The final 0.1.59 candidate includes F47 and F48; its installed continuation
is recorded below.

The 0.1.58 continuation completed its direct response and saved three exact
versions: the site `artefact_version_1816f305-993b-4982-bc19-3f6b0f9bc4c5`,
test evidence `artefact_version_be8e6edc-c878-47ff-a025-404385f05ab7`, and
selective-build evidence `artefact_version_a87d5e21-63d6-41be-83cb-0a914e7145dc`.
The site SHA-256 is
`57dc46c34b38dfb81f2c35560015f3e0a397b5268147978fe55ef1ad1ff73743`.
The original 9,802,734-byte candidate still has its pre-continuation modification
time and SHA-256
`0749d97fbedc640a7b0b0192169f4acedd8ebc201651aeb9176e1b97bcb057f5`.
The model reported 22 passing checks and disclosed missing image-source records,
browser/assistive-technology testing, publishing approval and the blocked plan.
Its descriptive Scope text is not an executable campaign or canonical review
state. No composition, ScopeExecution or collaborator team exists yet.

The actual app opened and ran the saved site. Its three images rendered and
maker navigation worked. Independent browser checks confirm all three embedded
images decode at 1,200 × 900, navigation reaches `#makers`, the 390-pixel browser
viewport has no horizontal content overflow, and Stop returns to the saved
version. The narrow screenshot was visually inspected. This is not a real
mobile-device or assistive-technology audit. Evidence:
`campaign-gallery-ui-0.1.58.json` and its desktop/narrow screenshots.

F31 remains open: the continuation consumed 4,432,361 reported tokens across
43 model responses, including 2,249,216 cache-read tokens, ten image reads and
28 discovery calls. Coverage confirms all 43 responses. Useful saved output
and image recovery passed, but the continuing organisation is incomplete; this
is not an efficiency success. Final evidence is
`campaign-continuation-0.1.58-final.json`.

Version 0.1.59 completed its final Windows build and verified installation with
F47 and F48 together. Service SHA-256:
`bc3acadf13d54b1c7aa552149dc9083f52833c9f2f4e4238785759227dcf77c5`.
Recovery: `C:\Users\jfenech\.floe\recovery\before-0.1.59-20260905-final`.
All 1,415 earlier Events, accounts, configuration, grants, 31 Actor heads and
runtime bindings survived unchanged; integrity and foreign keys pass. Evidence:
`delegation-upgrade-0.1.59.json`.

At 10:58:31Z the actual browser continued only the unfinished organisation work,
explicitly preserving the saved site and evidence and avoiding repeated image
inspection. Event `evt_060005c6-170a-44e4-95bf-154b6a4def1f`, Delivery
`del_c3c434c2-497e-4e28-8072-34797009e773`, same campaign Context. The model
received the current revision in its refusal, corrected the request and created
the draft. It validated, simulated and published its first composition at
11:05:01Z, then revised the design to accept the existing review package. The
real app opened the plan from Organised work and accurately showed **Plan only**
and **Planned** steps. Execution and final outcome are still being checked;
publication alone does not establish an operating campaign.

The 0.1.59 continuation completed its direct response after creating two
collaborators and publishing three retained plan revisions. The current plan is
`revision_5fde1566-3335-40f7-a117-2b37d3973c86`. It started
`execution_95fc5cac-8d6e-4e37-8569-d7cd3e701e3c` at 11:08:29Z with the three
existing saved versions pinned. No image inspection or rebuild occurred. The
Event step completed, but its release-steward step is ready with one queued
Delivery and no runtime bundle. Floe's final claim that the organisation is
operational is not accepted: no downstream result has been produced.

**F49 — A resolved runtime binding may have no delivery address.** Both new
collaborators have published Actor definitions and resolved runtime bindings,
but `endpoint_id` is null. `listRuntimeEndpoints` excludes them, so the Bridge
cannot attach them. The Scope can nevertheless publish and queue its Actor
placement. The release-steward queue is
`q_cf707506-b858-404f-894b-322ebd0b7027`, for NodeExecution
`node_execution_fdf0ed6e-789d-47c0-87a3-3f2294132fb0`; its destination Endpoint
does not exist. The first repair to attempt is supported Actor/runtime
configuration, then correct any contract or validation that allowed this
unreachable state to appear ready. Do not fabricate an Endpoint or alter the
queue directly. Also verify Actor identity versus a separately bound Endpoint
when diagnosing Scope routing; equal identifiers in fixtures are not proof.

**F50 — Control operations confuse their origin with the selected execution.**
Floe's `scope.execution.inspect` receipt
`opinv_8979cde71fe5cd775b8ebe6cfae3b4c9` was refused because its original direct
conversation Delivery `q_80d3fae2-bd55-468c-bdaa-1256f704e8c3` did not belong to
the selected ScopeExecution. `resolveCanonicalInvocation` replaces the causal
execution with the target execution before checking the originating Delivery.
The need is to inspect/control permitted work from another Context while
preserving exact causal provenance and target authority. Do not bypass
provenance validation. The actual work view shows the review step Ready and the
execution Pending; Stop execution is disabled. Its availability needs separate
verification rather than assuming the model's refusal explains the UI too.

F31 evidence for this organisation-only continuation: 2,642,795 reported tokens
across 52 responses, including 1,420,800 cache-read tokens. This is a different
task from the gallery continuation and not a like-for-like savings comparison.
Evidence: `campaign-continuation-0.1.59-final.json`.

The temporary two-operation test grant was removed through canonical Actor
publication and revoked at 11:19:36Z. The campaign Floe head is now
`actor_definition_672a1c34-4ac9-4515-9770-c481ace81622`; other definition content,
its original grants and its model binding are preserved. No Actor references
the temporary grant and no grant depends on it. The three saved version
references and their content hashes still match. Evidence:
`campaign-scope-grant-0.1.58-cleanup.json` and
`campaign-preservation-0.1.59.json`. There are no active runtime Deliveries;
the one queued Scope obligation is retained. F46's durable permission work,
F49/F50 and the full campaign remain unfinished.

### Control repair after the 0.1.59 campaign continuation

F50 is reproduced by two integration attempts: inspection from a direct
conversation Delivery and from another ScopeExecution. Governance now retains
source provenance independently of the target. Policies, scoped roles, budgets
and approvals use the selected resource; receipts and audit retain the actual
origin. Output publication separately refuses a runtime that names another
NodeExecution. False source provenance still fails before the handler. Focused
Bus regression: 30 tests across five files pass; installed proof is pending.

**F51 — The client used obsolete execution states and revision values.** The
live Scope view disabled Stop for an `active` execution because it checked
`pending`, `running`, and `waiting`. Its revision helper also omitted the
canonical `state_revision`, which would make a Stop request stale. Three UI
regressions reproduced these failures. The client now shares the transport-safe
status and revision contract with the Bus. Active and waiting work have current
labels and a usable Stop control; ended work remains protected. Nine relevant
client tests and both type checks pass. This does not make the unreachable
reviewer operational: F49's queued work and the saved gallery are preserved.

Installed 0.1.60 completed both installer bundles, passed the isolated image,
Command and Extension checks, and retained all 1,418 earlier Events, 33 Actor
heads, runtime bindings, accounts, grants and configuration. Integrity and
foreign-key checks pass. Recovery is
`C:\Users\jfenech\.floe\recovery\before-0.1.60-20260905-final`; installed service
SHA-256 is `8c3968365e65e8bf2763455b14325a98557d3eb52cc93ae4b326db89fb8a98ce`.
The source-served browser now displays Active and an enabled Stop control.
The preserved campaign was not cancelled to test the button.

At 11:50:05Z a fresh browser conversation asked Floe to recover the existing
review handoff without rebuilding or creating a new run. Delivery
`del_78ceeea7-0951-41e0-a430-2cf1e07f8c7e` was acknowledged. Its canonical
`scope.execution.inspect` receipt `opinv_7b4199654f4a8254e46e0f4162275927`
completed with the selected execution as target and the new conversation's
Event/Delivery as origin, with no invented originating ScopeExecution.
Pause `opinv_ed00cd5b80c251a994f8a571caebec64` and resume
`opinv_4e32b93cf658df49d770d9b1f12dc7ff` also completed/accepted from that
conversation. F50's installed reproduction is repaired.

Floe inspected the existing Actor and runtime binding, then confirmed that
direct notification fails with `context_recipient_unavailable` because the
reviewer has no Endpoint. It reported zero attempts and zero output publications.
No reviewer Delivery was created. F49 remains open. The recovery used 23 model
responses and 716,509 reported tokens (329,856 cache-read); no images were
reinspected. This is a different task from the earlier organisation work, not
a controlled efficiency comparison. Evidence:
`campaign-continuation-0.1.60-final.json` and `delegation-upgrade-0.1.60.json`.

**F52 — Successful tool results omit references needed for the next action.**
The model guessed revision `2` from the inspected execution and its first pause
was refused; the receipt's exact target reference was present only in tool
details, which Pi does not send to the model. Changed references after a write
were hidden the same way. The tool now returns compact result JSON together with
canonical target, changed, progress, cancellation and audit references when
present. It does not manufacture revisions or retry operations. A regression
failed before this change and now carries the exact returned revision into a
follow-up invocation. Ten Bridge tests and its type check pass; installed
recovery with this additional correction is pending.

Installed 0.1.61 includes the F52 reference projection. Both bundles and the
isolated packaged host checks completed. Recovery is
`C:\Users\jfenech\.floe\recovery\before-0.1.61-20260905-final`; the installed
service hash is `7c5e5b1c07dacd45d1201bdaa199bae0fc8298cd1078cd3393b6c3843ebf1863`.
All 1,420 earlier Events, 33 Actor heads, bindings, grants, accounts and
configuration passed preservation and integrity checks before the next attempt.

At 12:03:28Z the browser continued the recovery Context
`ctx_fea089d9-7a55-4c60-8cef-950782790f97`, asking Floe to repair the existing
runtime binding. Delivery `del_f5dedda3-8648-4da3-abfa-d6ae0f5778f9` was
acknowledged. Floe discovered the available configuration and replaced only the
reviewer's binding through `opinv_dfcd8ec43cad3737a0189a6a45eb48ac`, producing
`actor_runtime_binding_c7afd3fa-aade-41a2-8713-a09787e38929`. Its delivery address
is the existing reviewer Actor ID; the profile, Actor definition and grants
were preserved. The Bridge attached the Endpoint and claimed the original
queued package as `del_38c18522-07e6-49bf-9ef4-eb80e65e939e`. F49's missing
address was repairable through existing configuration. Separately addressed
Actor/Endpoint routing and prevention of misleading resolved bindings remain
unproven; the producer still has no address. No configuration contract was
changed in this slice.

No semantic operation was refused during that continuation or its one-shot
follow-up. The configuration turn used 312,946 reported tokens over 18 responses;
the follow-up used 129,377 over eight responses. This is not a controlled F52
efficiency comparison. The reviewer produced no model response.

**F53 — A collaborator's account permission was omitted.** The reviewer Delivery
was deferred before its first model response with `runtime_credential_unresolved`.
Read-only grant inspection confirms its published definition has only a general
workspace grant, with neither `credential.use`/`credential.refresh` nor the
account-purpose constraint. Floe's own separate account grant is present and
working. Both new collaborators omitted this permission. The generic account
error hides the difference between missing credentials and missing authority.
The existing delegation operation preserves account-purpose constraints; first
attempt the narrow supported delegation/configuration path, not another sign-in
or implicit account access for every Actor.

**F54 — Deferred runtime setup leaves the logical work falsely active.**
On 0.1.61, `reportDeliveryStatus("deferred")` closed only a pending ExecutionAttempt. The
Bridge reports injection before model authentication, so this failed setup had
already marked `attempt_b5fa5947-4d96-4fa6-8bb7-71c12417eaf8` running. The
Delivery is now deferred and the original queue row is retained, but the
NodeExecution remains active and the attempt running. Floe's one-shot follow-up
therefore reported no canonical blocker and work still in progress. Correct
the lifecycle projection and test both preparation and post-injection failures;
do not automatically replay a turn that may already have produced effects.

The 0.1.62 correction closes either a pending or running prepared attempt when
the Bridge reports setup deferral. One transaction records the failed attempt,
blocked NodeExecution and ScopeExecution, held inputs and deferred Delivery.
Endpoint reattachment cannot replay those inputs; explicit retry keeps the
original pins. The work view refreshes from the pushed deferral. Both preparation
states reproduced before the fix; 35 Bus lifecycle/retry checks, 98 Bridge
runtime checks and the three work-view checks pass. The view's missing refresh
also reproduced before correction. This does not rewrite the older stranded
attempt.

Installed 0.1.62 completed both bundles and packaged host checks. Recovery is
`C:\Users\jfenech\.floe\recovery\before-0.1.62-20260905-final`; installed service
SHA-256 is `e4b87f8f90a87ca533fa35fd99a5202d177997ec3f66fb7ea50448e72d89990c`.
The service on 5377 is the installed `C:\Apps\Floe\floe-node.exe`. Database
integrity and foreign keys pass; 1,424 earlier Events, 33 Actor heads, bindings,
grants, configuration and account files survived. The source-served browser
reconnected automatically. The old queued input was reclaimed on restart and
dead-lettered during preparation because its retained NodeExecution was already
active (`del_5409b0f9-225e-4091-8406-4eed8f3961e2`); no model turn ran. This is
retained pre-fix state, not an automatically repaired historical attempt.

The real recovery request was submitted at 12:42:38Z as
`evt_34b67ff3-0a53-4d59-ae13-fbe841ea3bf4`, Delivery
`del_d852d004-1e70-4de5-ae28-3e3a9700d165`. It explicitly replaces the earlier
test's no-new-run constraint: delegate the reviewer's narrow account permission,
retain its other settings, stop the stranded review, and deliberately redo only
the existing package review with the same plan and three saved versions.
The collaborator review still requires proof.

The account correction succeeded through `capability.grant.delegate`
(`opinv_1a05409a348b68b0d1729d0c55f70b12`) and Actor publication
(`opinv_b6cb1118ff459e922d63de333851d615`). New grant
`capgrant_639e896c-d883-4bd5-baa9-8ccb4b6f4a89` delegates only credential use
and refresh from Floe's existing account grant, retains the exact account and
Workspace constraint, and expires with its source on 2026-12-04. The reviewer's
new definition is `actor_definition_617420df-f4b0-4a83-9e85-6442d4c21df9`;
all other definition content and earlier grants match the recovery copy.

Floe stopped the original execution with `opinv_0711cbf2c6b3437c5b23faf50cc9336c`.
It published plan `revision_4ac5fb16-9386-4ff9-9708-b8d5ce33fc7f`, changing
only the reviewer's definition reference and placement account grant. Nodes,
Ports, Edges and other configuration are retained. Redo receipt
`opinv_ff0f7b405a697abda80e5884e0a6b5cd` created
`execution_ffa1357d-7b02-4ad3-83ec-dc447448dd0b`, explicitly linked to the
original review. Node `node_execution_287158f5-dffe-41f2-9bf8-7dac5bbdf413`
received the same saved package and ran using the existing account. Its first
model Delivery `del_d5ec9daf-963c-4577-a2cf-564c374478af` acknowledged after
11 responses. This proves account access, not completed gate publication.

**F55 — The model does not receive its current Scope work identity.** The
Bridge retains the prepared execution, Node, attempt and output Ports, but
`deliveryToPrompt` includes only the Context and input Events. The redo input
names its source Node as history; the reviewer used that cancelled source as
its output target. Its saved conclusion then requested Floe's help finding the
current work. The source correction projects a compact Current Scope execution
envelope from the Bus-issued contract, including exact active references and
output Ports. It does not preload grants, credential references or full runtime
configuration. The adapter reproduction fails before correction; 82 adapter,
guidance and pinned-contract checks and Bridge typechecking pass afterward.

**F56 — Inspection omits exact Node mutation references.** Execution inspection
returns a numeric Node `state_revision`, while mutation requires the canonical
opaque revision. Floe repeatedly supplied the counter and later intentionally
provoked a refused write to discover the full revision. Requests between actors
then changed that revision before publication. The source correction adds each
Node's canonical `resource_ref` to the existing execution inspection result and
describes its use. It changes neither authority nor the inspection target.
The missing-reference reproduction fails before correction; 25 operation and
governance checks and Bus typechecking pass afterward.

Installed 0.1.63 completed both bundles and packaged host checks. Recovery is
`C:\Users\jfenech\.floe\recovery\before-0.1.63-20260905-final`; installed service
SHA-256 is `a42c9d220c3acbc1b3f901532389d01b3f9efa02abef9fb779471b89268d7c3e`.
Integrity and foreign keys pass; all 1,455 earlier Events, 33 Actor heads,
bindings, grants, configuration and account files survived. Native-broker
inspection `opinv_c4fbfb5180dc50ee8f0b19ca7d28fa1a` returned the reviewer's
exact `resource_ref` revision ending `:14:waiting_external::`; this proves the
installed F56 read path. The source-served browser reconnected automatically.
The 13:23:48Z operator continuation (`evt_662b7aa5-25f2-48e8-a1f2-da7d9755e29a`,
Delivery `del_9a6a7c6b-102c-4a1d-b296-3fbbdb09f8d4`) requests only the retained
gate result, permits deliberate redo if stopped requests prevent continuation,
and bounds recovery to one refreshed publication retry without status or
reference-request loops. F55's live Scope envelope still requires a new Scope
runtime attempt; the successful continuation below used a direct Actor request.

The 0.1.62 attempt ended without a gate publication after 15 Deliveries and
222 model responses: 5,167,425 reported total tokens, including 1,910,912 cache
read tokens. Ten semantic operations were refused. This is a failed-outcome
baseline, not an efficiency success or controlled model comparison. An earlier
Floe response also dead-lettered on WebSocket closure. The repeated reference
loop was stopped through the installed native authority broker and the shared
`runtime.delivery.cancel` operation, receipt
`opinv_54e07df94f4980bbef1088a008899e90`, cancelling
`del_2b4bd9c7-f7fe-4179-9362-b67b9ab87685`. A prior stop request arrived after
its target had acknowledged and correctly did nothing. No active Delivery or
scheduled campaign pulse remained. The redo stays waiting for its unrecorded
output; original pins, all three saved versions and the revoked temporary Scope
grant remain preserved. Evidence: `campaign-continuation-0.1.62-final.json`,
`review-recovery-0.1.62-observe.json`, `campaign-preservation-0.1.62.json`,
`stop-reference-loop-2-0.1.62.json`, and `delegation-upgrade-0.1.62.json`.

**F57 — The source CLI cannot select the live Workspace.** Its normal operation
command reports an invalid local Workspace binding after finding the installed
native broker. The same `CliOperationClient` can discover and invoke the shared
operation using the already-verified Workspace identity and native broker.
No credentials were extracted or authority bypassed. The CLI selection/parser
failure remains an observed gate for the public CLI experience.

**F57 update — corrected in the 2026-09-06 app participation revision.** The
parser now consumes the broker's canonical flat `binding_id`/`locator` projection.
The actual CLI selects the registered Workspace from its folder and discovers
operations through the installed broker. Missing bindings remain unbound and
malformed records fail closed. See [current proof](actor-participation-revision-20260906.md).

An additional work-view observation remains: after a model turn finishes
without publishing, the selected Node can continue to display Working while
the execution says Waiting for a response. Its subscription handles setup
deferral but not all runtime settlement events. The runtime record and the
view's refresh need to be compared in the next legibility attempt.

**F58 — The reviewer was not granted execution inspection.** On 0.1.63 the
reviewer received a direct request to record its retained conclusion, but
`scope.execution.inspect` was refused with `operation_grant_required`. Its
general grant includes approval, Artefact inspection/search, Context emit and
Node output publication, but no execution inspection. This is an Actor
configuration correction using existing delegation, not a reason to grant
implicit read access. The repeated reference handoff was stopped with
`opinv_f89aca87c875402fe8c0e51bcc29ab36`, cancelling Floe Delivery
`del_f734bfc7-45c1-43b1-9d61-235004fe269c`. A subsequent operator request asks
Floe to delegate only the missing inspection permission, retain the reviewer's
other settings and account access, and have it read its own current reference
before publishing, with one refreshed retry.

The inspection grant correction completed with delegation receipt
`opinv_342dc589786a4651c06eda1ebf029463`: grant
`capgrant_2f0c6afb-3022-4299-86a7-f511cc3300f5` adds only
`scope.execution.inspect`, expiring with Floe's source grant on 2027-01-01.
Actor publication `opinv_2638c6ec8c7701c4fc5da1e69e778c9f` created
`actor_definition_3a216e51-6fff-42bf-a8ba-d8e8c1846441`. Existing grants and
all other Actor content are unchanged. The plan and original Node pins were
not rewritten by this correction.

**Installed review outcome — 0.1.63.** The reviewer inspected the execution
itself and published without a refusal, receipt
`opinv_28a13a0f210320028349ca3a38a2fdc6`. Publication
`publication_ef6c8fa8-c345-45c6-8bf7-1ef790cfa437` emitted
`evt_4195642e-21d2-48c4-86fb-596285bb7494` through `port_release_output` at
13:34:15Z. Its type is `campaign.release.awaiting-approval`; its content binds
all three exact saved versions and hashes, records 22 passing checks and the
retained visual review, and discloses missing image provenance/licensing and
full accessibility evidence. No external publication was authorised or done.
The review Node and redo execution are now completed. Floe verified this with
`opinv_8160aa75211fd04802d017aa76290231` and returned the outcome in the original
recovery conversation. The browser shows that result and, on opening organised
work, Complete with Stop disabled and the original cancelled execution retained.

The successful reviewer Delivery `del_f48e9b47-953c-4a81-837b-00d50c79a86c`
was a direct Context request using its updated Actor definition. It published
as the assigned reviewer through the shared operation; it did not replace the
Scope Node's historical runtime pins. This proves the F56 inspection-to-publication
path and F58 permission correction. It does not yet prove F55's new Scope
envelope in a real model turn.

The final permission-repair, reviewer and verification segment used 29 model
responses and 471,701 reported tokens; the reviewer used seven responses and
108,875 tokens. The complete 0.1.63 attempt, including the earlier denied
inspection and stopped handoff, used 92 responses over nine Deliveries and
1,349,160 reported tokens, including 406,400 cache read tokens. Two semantic
operations were refused. These are observed costs with two steering requests,
not a controlled savings claim. Routine handoff cost remains a product concern.

All three original version references and content hashes still match. The
temporary Scope grant is revoked, unreferenced and has no dependents. No active
runtime Delivery remains. The work view still foregrounds execution identifiers
and internal evidence labels; the conclusion and meaningful references need
further operator legibility work. The gate Event contains exact version
references in its content but has no canonical output Artefact attachments.
Evidence: `campaign-continuation-0.1.63-final.json`,
`review-recovery-0.1.63-observe.json`, `campaign-preservation-0.1.63.json`,
`release-gate-0.1.63.json`, `installed-node-reference-0.1.63.json`,
`delegation-upgrade-0.1.63.json`, and the actual browser conversation/work view.

The original NodeExecution pins the old reviewer definition and binding.
Changing a current Actor head must not silently rewrite that retained meaning.
When recovering its missing permission, inspect the supported retry/redo
contract; a fresh attempt is not permission to alter historical pins. A
deliberate redo of only the affected work may be required after configuration
is corrected. Preserve the original run and exact saved versions as evidence.

Final 0.1.61 checkpoint: all three saved references and content hashes match,
the temporary Scope grant remains revoked and unreferenced, no runtime Delivery
is active, and no build or installer is running. The one-shot follow-up has
finished. The review remains incomplete. Evidence:
`campaign-continuation-0.1.61-final.json`, `campaign-preservation-0.1.61.json`,
`campaign-runtime-current.json`, and `delegation-upgrade-0.1.61.json`.

### Nested review attempt on installed 0.1.54 — 2026-09-05

At 08:30:14Z, the actual browser started
`ctx_dc513c38-fc04-46ed-a7d2-c86a237c63f4` with a release-readiness outcome:
organise a review lead to commission an independent win/timeout replay check,
evaluate the returned evidence against the saved game/brief, and return one
concise go/no-go recommendation through Floe. Existing blind reviewers must
retain their independence; all files and saved versions must remain unchanged,
with no new review saved or external publication. Root Event:
`evt_146a2d8a-686a-4390-bcc8-1312f0b99450`; Floe Delivery:
`del_212e7be1-492e-4bc8-b2f4-f6f97f62acfc`. Baseline:
`nested-review-before-0.1.54.json` (20 files, 17 saved versions).

This attempts F38's remaining nested-return control case through useful work.
The existing auditor explicitly forbids consulting other reviewers; Floe
therefore created `three-seeds-release-readiness-lead` instead of overriding
that constraint. A separate real browser records changing Stop controls against
read-only Delivery observations in `nested-review-ui-0.1.54.json`, including
screenshots. It uses DOM changes rather than substrate polling, with no mocked
responses, invented outputs or model interception.

The work completed at 08:36:18Z with five acknowledged Deliveries. Floe created
the lead, which requested Lyra's independent check with the exact saved game and
brief. Lyra returned her findings; the lead assessed them and returned a concise
GO recommendation through Floe, explicitly excluding browser-level input, focus,
rendering and audio from its testing. Final Event:
`evt_ab4b0355-e475-4705-9c9a-ac8a6ca386da`. All 20 files and 17 saved versions
remain unchanged, with no new version or ScopeExecution. All 28 existing Actor
heads and runtime bindings remain unchanged; only the new lead was added.
Evidence: `nested-review-completed-0.1.54.json` and
`nested-review-actors-0.1.54.json`.

The attempt took 6m04s and 948,154 tokens across 40 responses: Floe setup
724,079/23, lead request 8,971/2, Lyra 111,366/6, lead assessment 92,669/7,
Floe return 11,069/2. Two failed tool calls are included; neither required
developer intervention. F31 remains open: Floe read the game and two test files
before delegating, and its model input grew from roughly 5,000 to 47,000 tokens
over 23 responses. These observations locate avoidable duplicated inspection;
they do not establish the savings from a correction that has not been tested.

**F38 nested-return gap reproduced:** The original conversation showed Lyra's
progress and Stop control, then lost both when the lead resumed as
`del_f5ed1dbf-50a7-4d77-8117-cbecef04afae`. The live browser record shows active
lead work with no Stop control from 08:34:49Z until Floe's own return at
08:36:07Z. Actual CUA inspection confirmed the missing controls. Returned work
was durably present, but the Context projection followed only outgoing requests.

The correction follows each canonical returned result back through its exact
request to the requesting Delivery. It adds no new relationship or lifecycle,
preserves Workspace boundaries, and serves both list status and detail controls.
The new regression failed before the fix. The same continuation regression also
exposed an outstanding inbound request left pending after cancellation; Stop now
closes that exact request while preserving the completed dependency. Tests cover
reopen, cancellation, late-result refusal and a forged cross-Workspace return.
All 38 relevant Bus checks, Bus TypeScript and the Windows build/package checks
pass. Installed 0.1.55 matches service SHA-256
`192210860972fc42d74436286bbe47f4e76c5f29537c7bc9d559e4be845a3ab8`.
Recovery is `C:\Users\jfenech\.floe\recovery\before-0.1.55-20260905-final`.
The 1,392 earlier Events, account files, configuration, grants, all 29 Actor
heads and runtime bindings are retained; integrity and foreign keys pass.
Evidence: `delegation-upgrade-0.1.55.json` and `actor-heads-upgrade-0.1.55.json`.
An earlier maintenance attempt stopped the idle app before packaging had ended;
the installer was absent and did not run. The existing app was immediately
restarted, the first verified backup was preserved, and the final helper added
installer/packaging preflight before taking a fresh backup and installing.

At 08:46:57Z the actual browser submitted **Repeat the release-readiness review
with the existing lead.** in the same originating Context. Event:
`evt_6affd6f9-892b-4bec-9896-ef7d94cde99f`. It asks the same lead to commission
Lyra's independent exact-version replay check and evaluate the returned evidence,
with unchanged responsibilities/access and no file, version or publication
changes. The baseline is `nested-return-stop-before-0.1.55.json`; the live browser
record is `nested-return-stop-ui-0.1.55.json`. The intended intervention is Stop
while the lead evaluates returned work.

The repeat completed on 0.1.55 with all five Deliveries acknowledged. The browser
record now shows **Stop Three Seeds Release-Readiness Lead response** throughout
its returned assessment (`del_24b7d0e4-7732-4b60-80a0-15a6e8fac927`), then
**Stop Floe response** on Floe's return, and no working controls after completion.
All files and saved versions remained unchanged. Cost: 356,883 tokens across
31 responses, about 5m20s, with no failed tool calls or developer intervention.
This reused an existing lead; it is not an isolated efficiency comparison with
the earlier creation attempt. Evidence: `nested-return-completed-0.1.55.json`
and `nested-return-stop-ui-0.1.55.json`. The manual Stop opportunity was missed;
this proves visibility and completion, not cancellation.

A further actual composer request starts **For the final replay check, use the
existing lead and Lyra.** It repeats the same read-only check with unchanged
responsibilities and access. The browser observer is now configured to click the
visible Stop control once, specifically when the existing lead has resumed on
a `request.result` Delivery from this attempt. It does not invoke a hidden API
or alter browser state. Evidence uses the separate `nested-return-auto-stop-*`
baseline, observer and state helpers.

**F38 returned-response Stop/restart proved on installed 0.1.55:** Root Event
`evt_11629ecc-3d9a-4c09-8569-f04801c43eda` was submitted at 08:54:19Z.
Lyra completed `del_e80d8268-f1c4-481a-b935-e6e84e2b63de`; the lead resumed as
`del_502272b3-1a6a-4ef9-91a3-7bb35ab642c5`. The real browser observed that
returned Delivery with **Stop Three Seeds Release-Readiness Lead response** and
clicked the visible control at 08:57:14Z. Cancellation committed at 08:57:15Z.
The original conversation showed **This response was stopped. Changes already
made remain.** with no active response control.

After a host-controlled restart of the installed app/runtime, the returned
Delivery and queue entry remain cancelled, its authority remains revoked, and
its exact inbound request `evt_1e582f97-148a-4ad2-abdb-f48b99e050ea` remains
cancelled. Lyra's dependency request remains resolved and its Delivery
acknowledged. No late success or active work exists. The actual browser list
still shows **Stopped** and distinguishes historical progress with **Last
message:**. The native restart interaction itself is not claimed.

All 20 files and 17 saved versions/content remain unchanged; there are no added
files or versions. Evidence: `nested-return-auto-stopped-0.1.55.json`,
`nested-return-auto-restarted-0.1.55.json`,
`nested-return-auto-restarted-preservation-0.1.55.json`, and the real browser
observer's action/state record and screenshots. Nine documentation checks pass.
The stopped attempt reports 171,209 tokens across 17 responses, including one
aborted response reporting zero tokens; this does not establish zero provider
cost for the aborted request. It took 2m56s to the deliberate Stop. This is
cancellation proof, not another completed review or a savings comparison.

The next proving experience is the original campaign-site journey in
`end-state-verification.md`, building on the completed direct/nested collaboration
and recovery increments. Full acceptance gates remain Pending, including
headless continuity, native/mobile interaction and the complete 18-step scenario.

### Conversation status and stopped-review continuation — 2026-09-05

**F41 — The conversation list presents historical progress as current work.**
Before the deliberate F38 continuation, the actual browser still previewed the
Naive-Path Playtest Auditor as running although its Delivery was cancelled and
its authority revoked. The smallest need is a current response label beside the
historical preview. This is a client projection of existing Delivery state,
not a new work lifecycle or an edit to retained messages.

Installed 0.1.54 batches a Context list's Delivery summaries, using the same
explicit request ancestry as the conversation's response controls. Active work
shows **Working**; latest cancellation shows **Stopped**; failed or deferred work
shows **Needs attention**. A labelled preview begins **Last message:**. Delivery
pushes update the list; lease renewals and telemetry do not trigger list reloads.
Tests cover cancellation across reopen, fresh continuation without reviving old
work, unrelated and cross-Workspace exclusions, API projections and live client
updates. Eight runtime Delivery tests, 21 related API tests and 49 conversation
tests pass; the final list-focused run passes 22 tests. Bus TypeScript and the
Windows build/package checks pass.

The actual browser on 0.1.54 showed **Stopped** and **Last message:** on the
original unchanged review row. Recovery is retained at
`C:\Users\jfenech\.floe\recovery\before-0.1.54-20260905`.
The installed service matches SHA-256
`cf5d416d1d6638b21ed9c6f8be94043ba6d15ddf21a3cb71f4c2c77368c18847`.
All 1,377 earlier Events, grants, account files, configuration, 28 Actor heads
and runtime bindings survived; database integrity and foreign keys pass.
Evidence: `delegation-upgrade-0.1.54.json`, `actor-heads-upgrade-0.1.54.json`,
the `conversation-status-*` check logs and actual CUA views.

At 08:22:40Z the actual composer submitted **Continue the review I stopped.**
in `ctx_1a8d7ec1-b2b3-47e3-92b8-419ffda64a29`, asking the same auditor to finish
win, timeout and replay checks, reuse useful completed evidence, return one
concise finding and testing limits, and preserve all files/versions without
saving or externally publishing another review. Event:
`evt_01411b7f-5490-418c-a519-def1efd66623`; fresh Floe Delivery:
`del_55660df9-2c91-4057-8926-1190daf94845`. The list changed to **Working**.
The 20-file/17-version baseline is `continued-review-before-0.1.54.json`.

**F38 continuation completed:** Floe submitted a fresh request with the exact
saved game and brief. The same auditor completed
`del_f35191e5-8878-4bf3-8a63-a07a4694dad4` in
`ctx_72e82232-abbb-4842-a4d3-a6515e9ac49c`; Floe returned through
`del_407064eb-7b84-4012-a89d-8feb6ec44d65`. All three Deliveries are acknowledged,
with fresh authority sessions and no ScopeExecution. The original cancelled
Delivery, queue entry and pending request remain cancelled; its original
authority remains revoked and no late success exists.

The auditor read the complete saved game in two portions and the brief, verified
their hashes against current files, then ran the embedded game model at 50ms
steps. The actual command results confirm wins, timeouts, four switch rhythms
and resets after both outcomes. Floe's concise final answer recommends immediate
feedback for productive and mistimed weather changes and names the model-level
testing limit. It arrived at 08:25:30Z as
`evt_97179c01-7706-43a1-a4fa-71136a171e73`; the actual browser displayed it and
removed working/Stop controls. All 20 files, 17 saved version rows and retained
content remain unchanged, with no new saved version; the three historical F20
mismatches remain unchanged. Evidence: `continued-review-completed-0.1.54.json`
and `continued-review-authority-0.1.54.json`.

The completed continuation took 2m50s and 225,588 tokens across 18 responses:
Floe setup 68,129/7, auditor 143,772/9, Floe return 13,687/2. There was no developer
intervention during execution. One failed browser-on-PATH lookup is included.
As in the earlier independent review, this lookup does not prove no browser is
installed: the auditor also checked three Node package names and did not discover
the available host browser. No browser or human playtest is claimed for this
review. Reliable Actor discovery of available testing capabilities remains an
observed limitation; this is distinct from Astra's actual browser proof.

A separate fresh-profile browser check during active work reached the list in
3,214ms and opened the conversation/model controls in 3,733ms. It received live
lease/telemetry pushes, with three list reads and three reads per conversation
snapshot resource, without replaying historical activity. This is one observed
run of the source-served browser against the installed backend, not native UI
or standalone packaged web-entry proof. Nine documentation checks also pass.
The full acceptance gates remain Pending.

### First publication on installed 0.1.52 — 2026-09-05

**F37 repeat completed on installed 0.1.52:** At 07:32:47Z, the actual browser
started a fresh conversation, `ctx_55c93db8-6f21-4571-b8d0-a292a4bd0af6`, with an
explicit request to create a new independent collaborator, review the current
saved Three Seeds game/brief without previous reviews, and return its review and
Floe's recommendation as a new named saved result. Existing files and saved
versions must remain unchanged; no external publication is authorised. Root
Event: `evt_ecb0ab69-f1d5-4821-9cdf-5e3a43cac166`; first Delivery:
`del_eeee7875-4e96-4247-b95f-1bc153aa38db`. The model is the existing
GPT-5.6 Sol/high. The browser showed work progress and Stop after submission.
The baseline contains 19 files and 16 saved versions in
`first-publication-before-0.1.52.json`; observations use the separate
`floe-first-publication-proof-20260905.mjs` helper. This repeats the clarified
outcome in a new Context; any efficiency comparison must account for the reduced
conversation history.

Floe created `three-seeds-first-visit-observer-lyra` (Lyra Vale — First-Visit
Observer), delegated two grants at 07:34:40Z while its published head was null,
then published revision 1 with both grants at 07:35:02Z. No empty publication,
second publication or developer intervention was needed. Model input mistakes
caused two schema refusals and two corrected-input/idempotency conflicts before
autonomous recovery; these four failed tool calls remain included in the cost.
Evidence: `first-publication-authority-0.1.52.json`.

Lyra completed `del_9631cf85-80cb-43cb-b2ec-2d2bcbae816e` in
`ctx_a040a0ce-0c31-44d7-8c08-2b4390a420c2`. It inspected only the game and brief,
ran both saved Node checks and a read-only timing simulation, and explicitly
left browser interaction and first-player comprehension untested. Floe resumed
as `del_2a575536-b7c0-4edb-879c-e9e9ca5b4e15`; the originating conversation showed
working/Stop controls for the collaborator and then Floe's returned work.

The actual named Open control displayed **Three Seeds — First-Visit Goal Review
and Recommendation**, version `artefact_version_6daeab86-2395-4bd7-8148-22d7aac3f581`,
4,427 bytes, SHA-256 `4c050faff4215d1bedac721cc6ba4c89c561756044e6ac905b740c20db92bcd1`.
It recommends stating the all-three-plants/one-minute objective before play and
separates evidence, inference, tests and limits. All 19 earlier files and 16
version rows/content remain unchanged; the three historical F20 mismatches remain
unchanged evidence. The final response landed at 07:38:35Z: 5m48s after submission.

Usage was 783,905 tokens across 38 responses: setup 407,594/16, reviewer 73,087/5,
Floe return 303,224/17. This is lower than the previous 1,155,399/53 successful
new-identity continuation, but the Context and reviewed finding differ; it is not
an isolated causal estimate of F37's savings. Evidence:
`first-publication-completed-0.1.52.json`, its preservation baseline and actual CUA
views. F37's corrected first-publication path now has installed model proof.

**F40 — A direct work request drops exact saved inputs.** The actual request
`evt_7022efd1-57b2-46df-9ec4-64c6f28bc33f` contains file paths but an empty saved
version list. The runtime's `request` tool accepts only actor/work and hardcodes
`artefact_version_ids: []`. Its recipient therefore cannot receive selected
saved inputs through this dependency path. Floe verified matching saved digests
after the review; that does not pin the request's historical inputs.

The smallest need is to carry selected immutable input references with dependent
work. This belongs in the runtime adapter over the existing Event contract, with
actor guidance to use it, not a new substrate concept. Installed 0.1.53 lets
`request` attach exact IDs, reports accepted references, and keeps omitted inputs
empty rather than inheriting unrelated references. The existing Bus validates
them; refusal does not mark a dependency as accepted. Runtime guidance directs
recipients to read exact saved content in bounded portions. The adapter regression
failed before the fix; 97 relevant Bridge checks pass, including existing saved
content paging, plus refusal-then-retry checks. Bridge TypeScript and the Windows
build/package checks pass. The verified recovery copy is
`C:\Users\jfenech\.floe\recovery\before-0.1.53-20260905`. The installed service
matches SHA-256 `0be3fbf0acfeb248504a00d936fa20ea8d3254f0e072811b4b65c1cc0bfa5a2e`.
All 1,371 earlier Events, account files, configuration, grants, 28 Actor heads,
Floe's runtime binding and Lyra's runtime binding survived; integrity and
foreign-key checks pass. Evidence: `delegation-upgrade-0.1.53.json` and
`actor-heads-upgrade-0.1.53.json`.

**F40/F34 installed proof:** At 07:49:14Z, the actual browser asked Floe to have
Lyra read the complete saved game and brief, answer about the welcome/win/timeout
behaviour, and preserve all work. Root Event:
`evt_a7d8a2f7-5f1f-43ba-85d0-59d7a4dbbd5d`; originating Context remains
`ctx_55c93db8-6f21-4571-b8d0-a292a4bd0af6`.

The dependency Event `evt_af9d3530-40c8-4f42-ae0c-62bbb3f792e3` now pins both exact
saved input versions. Lyra received them in
`ctx_58f449fe-b840-4b1f-8a7a-13c40fba94d0`, Delivery
`del_ea8428fb-f099-44ce-bfb1-36e8c954b797`. It used `read_artefact` at offsets 0
and 16,000 for the game, covering all 25,156 text units / 25,160 bytes, and read
all 2,421 text units / 2,453 bytes of the brief. Four subsequent focused rereads
also succeeded. Matched Before/After tool evidence and retained content digests
confirm coverage of both inputs, with zero workspace-file reads or command calls.
It did not substitute current files for the saved inputs.

Floe resumed through `del_6aeaf37c-ffae-40af-bb22-2cd428e9d52e` and returned the
answer at 07:51:10Z, 1m57s after submission. The real browser displayed the answer,
exact version names, static-inspection limits and completed state. All 20 earlier
files and 17 version rows/content remain unchanged; no new version was created.
The attempt used 151,266 tokens / 15 responses: setup 71,380/8, Lyra 66,231/5,
return 13,655/2. There were no failed tool calls or developer interventions. This
bounded inspection is a different outcome from the full saved review and must
not be presented as an equivalent efficiency comparison.

Evidence in the existing upgrade proof directory:
`saved-input-handoff-completed-0.1.53.json`, its baseline,
`saved-input-pages-0.1.53.json`, `request-saved-inputs-red.log`,
`request-saved-inputs-green.log`, `request-saved-inputs-final.log`,
`request-saved-inputs-types.log`, `build-0.1.53.log`, and actual CUA views in this
task. F40's direct request handoff and F34's real paged reading are now proven.
The next bounded continuation proof is deliberate resumption after the previously
stopped collaborator response; nested return controls, document-table legibility,
native/mobile proof and the complete acceptance campaign remain outstanding.

### Fresh independent collaboration on installed 0.1.49 — 2026-09-05

Original request `evt_b5efccaf-8773-4454-af91-c6e8ab9cafc2` in
`ctx_1a8d7ec1-b2b3-47e3-92b8-419ffda64a29` said “fresh independent collaborator”.
Floe chose an existing unused reviewer whose historical grants belonged to Floe.
The request was refused with the correct grant-principal reason. The developer
stopped Floe through the actual Stop control and clarified that this attempt
required a new identity (`evt_a5a4a81e-d90f-443c-be3b-df738aa92436`). This is one
test clarification/intervention; the original wording did not unambiguously
require a newly created Actor.

Floe created `three-seeds-naive-playtest-auditor`, delegated bounded access,
published its definition and attached its model without restarting the runtime.
The new Actor completed `del_2baffe18-4394-41ff-913c-231e248f04d6` in a separate
Context. Floe resumed and saved **Three Seeds — Naive-Path Review and Recommendation**,
version `artefact_version_bd6bcc62-a5f2-49fa-884a-350fda03351f`, 4,686 bytes,
SHA-256 `4d0a41a345e2f813a98434802f879d4380f98a0830e23cf195dd6b0b65d7e16d`.
The review recommends immediate feedback for good/poor weather-switch timing.
It records deterministic tests and explicitly leaves live perception, browser,
touch and accessibility verification open. The named attachment appears in the
actual conversation. Its named Open control displayed the complete review after
the 0.1.50 update. The compact Markdown renderer displayed its table as raw
Markdown; document-table legibility remains an observed presentation gap.

The successful new-identity continuation used 1,155,399 tokens across 53 model
responses: 677,570 for setup, 99,735 for review and 378,094 for Floe's return.
Including the initial request and stopped recovery, the attempt used 1,288,562
tokens. The successful continuation took 8m20s; this does not prove an efficiency
improvement over the earlier reuse of an existing reviewer. All 18 pre-existing
files and 15 saved-version records/content remained unchanged. The three known
historical F20 mismatches remain separately reported, not repaired or concealed.
Evidence: `fresh-collaboration-observe.json` and its baseline in the external
proof directory. F35's failed grant attempts were correctly recorded as failures.
The reviewer read current source paths, not paged exact saved text, so F34's
continuation feature remains source-tested without a live long-text paging proof.

**F37 — New Actor access requires a revision that does not exist.** The shared
delegation operation required a current published Actor revision, despite its
description telling callers to delegate before publication. Five attempts were
refused before Floe worked around it by publishing an empty-grant definition,
delegating, then publishing again. The source correction accepts an exact null
revision for an unpublished Actor and still refuses when a published revision
differs or has appeared meanwhile. Interactive and unattended authenticated
tests now delegate before the first publication. This corrects an existing
operation contract; it adds no primitive or automatic grant policy.

**F38 — Requested work disappears from the originating conversation's controls.**
While the new reviewer was active in `ctx_1cff0f25-bd0c-4df4-b38c-7b85c56b5aef`,
the original conversation showed “review is underway” but no working indicator
or Stop control. A later supported browser cancellation invocation arrived after
completion and correctly returned `cancelled:false`, `state:acknowledged`
(`opinv_8480ce557083c27e52e58bdd8f529770`). No cancellation or restart proof is
claimed for that attempt.

The source correction projects existing `request_parent_delivery_id` ancestry
when inspecting a Context's responses, prioritises active work before the
history limit, and retains separate controls for distinct responses by one
Actor. The client refreshes on lifecycle pushes and restores state on reopening.
Stop still invokes the same exact-response operation and does not silently
cancel descendants. The tests include another Context, unrelated work,
cross-Workspace exclusion, completed parents and a stopped child after reopening
the database.

0.1.50 was installed after 884 Bus checks (one skipped), 224 app checks (two
existing TODOs), Bus TypeScript and the packaged Command/Extension checks passed.
The first broad parallel runs timed out; the app's affected checks passed alone
and its complete suite passed with two workers. The attachment-history upgrade
test still expected a schema-12 receipt; it now selects the actual latest receipt
and continues verifying the preserved schema-11 backup and frozen history.
Recovery copy: `C:\Users\jfenech\.floe\recovery\before-0.1.50-20260905`.
Installed service SHA-256:
`3035d1ed489175f769401241bcbd70266514e92901a836998e21b848ed0ad66b`.
The existing 1,360 Events, account files, configuration, grants, Floe definition
and runtime bindings remain unchanged; database integrity passes with no foreign
key errors. The browser temporarily reported unavailable services during the
busy build/check period, then returned through normal reconnect/reload; its
800ms startup-attempt timeout needs a bounded load test before claiming robust
startup under load. No credentials were replaced or manually entered.

**F38 installed Stop/restart proof:** Through the actual browser composer, the
operator asked the existing auditor to inspect win, timeout and replay behaviour
without changing files (`evt_57adba62-af61-4fd7-8023-2fb4369f3e26`). Floe requested
the work and completed its own response. The original conversation retained the
auditor's progress and **Stop response** control while its separate-Context
Delivery `del_15a15f66-253c-4eef-9ec6-3f5abf44608a` was `injected_to_runtime`.
The screenshot showed completed reads of the brief, game source and saved tests.
Clicking **Stop Naive-Path Playtest Auditor response** at 06:53:14Z cancelled the
active response. Its control disappeared and the conversation showed “This
response was stopped. Changes already made remain.”

After a controlled restart of the actual installed desktop/runtime, the same
Delivery and pending request remain cancelled, its authority session is revoked,
there is no active queue entry and no late success. The protected browser
inspection returns the same state. The restart was performed through the host,
not a native UI control; native restart interaction remains unproven. Existing
files and saved versions remained unchanged, including the newly retained review.
The attempt recorded 32,561 tokens across seven responses, including the aborted
response; child reasoning usage was unavailable. This is cancellation evidence,
not a completed review or an efficiency comparison. Evidence files are
`collaborator-stopped-before-restart-0.1.50.json` and
`collaborator-stop-restart-0.1.50.json`; actual UI screenshots are in this task.

**F39 — Reopening generates repeated reads before becoming usable.** On normal
browser reload, Conversations and then the selected conversation stayed loading
for tens of seconds while the service was healthy. The installed log around
06:51:08–09Z shows repeated identical Context-list requests completing in roughly
56–95ms. Startup also briefly reported unavailable services during the earlier
build/check load. Diagnose stream replay and refresh dependencies before choosing
a fix; the short startup deadline alone does not explain the repeated reads.
F37's corrected first-publication path was subsequently proved in a fresh model
attempt, and F34's long-text paging was proved on 0.1.53. Document-table legibility,
deliberate continuation and nested returned-work controls remain open.

F39 diagnosis: each new browser subscription sent a null replay cursor. The
authenticated stream therefore replayed all retained workspace activity, and
snapshot-based views requested fresh state for each old message. Opening the
conversation list on installed 0.1.50 again remained loading for tens of seconds.
The extra Playwright connector became unavailable (`Transport closed`); CUA
remained functional. Do not turn this into a substrate or account failure.

Installed 0.1.51 adds an explicit current starting position to the existing
authenticated stream. Snapshot clients opt in and read current state after the
subscription is accepted. Reconnect retains the accepted cursor, including when
no live message has arrived, and replays missed updates. Existing historical
replay and Bridge acknowledged checkpoints remain available; a Bridge cannot
skip its checkpoint. The native host now keeps a cursor per stream instead of
allowing another view to advance a shared workspace cursor. This corrects an
existing transport/client contract; it does not add domain state or a primitive.

Source checks pass: 17 authenticated stream/replay checks, 51 conversation/client
checks, six vocabulary checks, Bus TypeScript and 28 native checks. A test of
initial conversation loading was made to await its asynchronous request rather
than assuming the heading meant loading had completed. Upgrade verification
retained 1,364 earlier Events, configuration, account files, existing grants,
Floe's saved definition and runtime binding; integrity and foreign-key checks pass.

A fresh Edge profile opened the actual localhost browser client against installed
0.1.51 without manual pairing or account setup. The conversation list was usable
in 4,388ms; opening the stopped review and its ready model control took 3,219ms.
There were three Context-list reads and three each of Context, Event and Delivery
reads when entering the conversation. Cancellation remained visible with no Stop
button. These timings are one observed run, not a benchmark distribution or native
desktop proof. Evidence: `browser-startup-0.1.51.json/png` and
`delegation-upgrade-0.1.51.json` in the existing upgrade proof directory.

That same measurement exposed one remaining notification subscriber replaying old
activity: 3,241 lease-renewal and 3,253 runtime-telemetry frames, plus other retained
updates. Installed 0.1.52 makes this existing new-activity listener opt into
the current position too; its reconnect still replays missed events. The same
51 client/conversation checks pass. The Windows build and packaged host checks
pass. The verified recovery copy is
`C:\Users\jfenech\.floe\recovery\before-0.1.52-20260905`; installation preserves
the same 1,364 Events, accounts, configuration, existing grants, Actor definition
and runtime binding, with no integrity or foreign-key errors.

The identical fresh-profile browser journey on 0.1.52 opened the conversation list
in 3,211ms and the conversation with its ready model control in 2,686ms. Only
connection/authentication/catch-up frames arrived: zero historical activity
frames. The snapshot request counts remained three per relevant resource.
The actual Codex browser also reloaded and opened the test Workspace without
pairing, showed the existing ChatGPT model as Ready, and retained the stopped
response notice with no active Stop button. F39's repeated historical replay is
resolved for this browser journey. No model request or token spend was needed.

Evidence: `browser-startup-0.1.52.json/png`, `delegation-upgrade-0.1.52.json`,
`snapshot-stream-0.1.52-tests.log` and `build-0.1.52.log` under
`C:\Development\_temp\floe-upgrade-proof-20260904`, plus the actual CUA view in
this task. The browser frontend is still served from source at localhost:5379
against the real installed service on 5377; the installer does not yet publish
a standalone web entry. This is one observed timing run per version, not native,
mobile, production-hosted web, or full product proof. The later F37 repeat now
passes; deliberate continuation and nested returned-work controls remain pending.
All acceptance gates remain open.
The post-upgrade preservation check found no changed original files, saved version
rows or retained content; the newer independent review's SHA-256 also still
matches. The three previously documented F20 mismatches remain unchanged evidence.

## Outcome

Use Floe through its actual operator interface to establish whether it can accept an outcome,
produce useful work, explain that work, accept a correction, and continue across interruptions.
The development agent owns diagnosis and routine product judgement. Ask the operator for subjective
direction, required permission, or inaccessible real-world action.

This is a proving milestone within the complete product direction in MISSION.md, PRODUCT.md, and
accepted ADRs. It does not replace or close the remaining end-state obligations from the current
implementation handover. Retain each unresolved gap until it is fixed and verified.

## Ownership and starting evidence

The existing implementation task is `Simplify floe-app operator entry`
(`01a01f16-1077-79c0-94d0-3acbeab0ebf9`). On 2026-09-04 the operator requested Astra ownership.
The coordinating task is `Review Floe substrate progress`
(`01a06a7c-cd67-78b0-95aa-e1e19d10888a`).

The existing task was asked to finish its in-flight attachment change, bring other writers to a
checkpoint, report the full acceptance obligations and test evidence, and remain read-only.
Confirm that handover before changing shared runtime implementation or rebuilding active services.
Do not report the overall goal complete at that checkpoint.

Handover received in `astra-end-state-handover-2026-09-04.md`; previous writers are complete or
stopped. The originating task remains read-only across its automatic goal continuations.

Mission, product, and instruction corrections were applied separately. Template, prompt assembly,
and actor-neutrality checks passed (44 tests); documentation checks passed (9 tests). These are
source checks, not installed-app evidence. Existing workspace frontmatter was preserved while the
actor body and substrate reference were updated. Running or pinned instructions remain unverified.

The desktop build now includes the Command and Extension child processes and runs both using the
packaged Node executable outside the checkout. Echo invocation, unknown Command revision refusal,
Extension activation/invocation/shutdown, and absence of ambient Node/network globals passed.
Source-tree-only Extension read roots no longer prevent packaged startup. These checks do not close
the full Command, Extension, or installer acceptance gates.

The read-only attachment review identified five gaps. The following source corrections are now in
place; none substitutes for the installed-app and real-model proving attempt:

- **F1 — Shared images reach model input.** The prompt and Context history retain exact Event
  ArtefactVersion references. The shared runtime `read_artefact` tool retrieves digest-checked content
  through the same authenticated content endpoint as the app, using active Delivery authority. It
  presents supported images directly to the model and bounds text. Images are fetched on demand.
- **F2 — An uncertain send remains one send.** Both composers retain the prepared request and uploaded
  ingress references after a lost response. Retry checks the original receipt. The draft stays intact
  and cannot be edited while uncertain. Conversation creation uses the same retry discipline; a known
  created Context is retained when an upload fails before sending.
- **F3 — Retry identities stay within authority boundaries.** Two authenticated HTTP regressions
  reproduced a caller key returning another Workspace's or principal's Event. The backend now uses
  the registry's stable, authority-scoped invocation identity, with a second boundary check before
  replaying a stored Event.
- **F4 — Temporary uploads are bounded.** New upload activity reclaims expired bytes. Aggregate pending
  size/count and terminal history are bounded. There is no recurring cleanup timer.
- **F5 — Failed messages reclaim only new content.** Reference validation precedes content publication.
  A synchronous file-write or database failure rolls back the message and removes only files created
  by that attempt. Existing shared content is preserved; uploads remain available for an exact retry.
  Abrupt process/power-loss recovery remains part of the wider restart acceptance obligations.

Focused evidence: Bridge image/adapter/authority/client checks passed (94 tests), app communication,
composer and attachment checks passed (49 tests, including preparation recovery and simultaneous sends),
and Bus attachment/Context checks passed (28 tests). Bus, Bridge and app builds passed. The rebuilt
desktop bundle again passed isolated Command and Extension host checks. These are source and build
checks, not installer, live-model, visual, or end-state acceptance.

### A0 attempt on 2026-09-04

Launched the rebuilt desktop substrate in a separate temporary Floe home and connected a development
frontend to it. The substrate became healthy and rejected unauthenticated protected access. Codex's
in-app browser refused both the loopback and localhost frontend URLs with `ERR_BLOCKED_BY_CLIENT`
before any Floe page rendered. The operator opened the installed desktop app; a second browser
attempt remained blocked. No onboarding, conversation, model response or visual result was observed.

Stopped the temporary substrate and frontend after this attempt. The operator's installed substrate
on port 5377 was left running. No installed app, user credentials, configuration or history was reset.
The temporary test home is retained at
`C:\Users\jfenech\AppData\Local\Temp\floe-astra-operator-5gf6cb`.
After the final source checks and rebuild, restarted an isolated test instance to make the queued
Browser pane reviewable. It is healthy at `http://127.0.0.1:63068`, with the current frontend at
`http://localhost:5379/`; test home
`C:\Users\jfenech\AppData\Local\Temp\floe-astra-operator-RmgTrD`.
The test substrate is owned by terminal session 16475 (send `stop` to shut down its child), and Vite
by terminal session 69600. Codex again returned `queued` for the Browser pane. The operator was asked
to open this Codex task and its Browser pane, distinguishing it from the separate Floe desktop app.
No page has rendered in the computer-use tool yet.

Restore supported browser access, then repeat A0 with this known current build. Do not replace the
native authority broker with a test proxy to get past onboarding.

### A0 browser connection attempt on 2026-09-04

The operator made the existing Codex browser tab accessible. The first rendered
page showed a 401 response from `/v1/local/workspaces` as a stuck local service.
The desktop used its native authority broker; the browser sent unauthenticated
HTTP requests and its WebSocket used a hardcoded port without an authentication
frame. This was an actual missing browser transport, not a stopped service.

- **F6 — Browser connection repaired in source.** A bounded connection request
  is approved by the native host for one Workspace. An origin-bound HttpOnly
  cookie references a canonical authority session; native and browser sessions
  share the existing grant issuer and request verifier. The browser receives no
  reusable bearer or local Workspace locator. Its WebSocket authenticates before
  showing state and retains a cursor across reconnects. Refused startup requests
  are not repeatedly retried or described as stopped services. Browser provider,
  model, and local-file controls retain their explicit desktop boundary.
- **F7 — First-use conversation remains blocked.** Registered a fresh isolated
  workspace through the authenticated host API. Its `.floe/agents/floe.md` was
  created, but the UI displayed `The workspace operator is not available yet`,
  including after reload. The desktop bootstrap still attempts a legacy
  `registerEndpoint` call for the operator while the server restricts that route
  to a Bridge credential. Investigate the canonical connection between the
  authenticated principal, Actor and Context participation; do not restore a
  privileged legacy registration route or manufacture successful onboarding.

Actual browser proof: selected **Connect this browser**, observed the code,
selected **Continue** before approval and saw the correct refusal, approved
that exact request through the authenticated host API, then selected
**Continue** and opened `Astra browser proof`. Reload retained the connection.
The existing stream reported its connected runtime. **Disconnect browser**
revoked the connection and returned to the connection screen. The last visible
page is that disconnected screen.

The host-side test approval used the real authenticated approval endpoint, not
an HTTP authority bypass or a browser-injected credential. The native approval
command compiles and its surrounding native suite passes, but computer use
cannot operate native Windows controls here. No actual native confirmation,
provider login, model turn, or complete A0 success is claimed.

Current isolated service: `http://127.0.0.1:63068`, packaged sidecar PID 82032;
home `C:\Users\jfenech\AppData\Local\Temp\floe-astra-operator-bgJ5CD`;
workspace `workspace_59aaa31a-5ee4-4653-afa7-cfda9fed1c06`.
Terminal session 24865 owns the launcher (`stop` ends its child). The source
frontend remains at `http://localhost:5379/`, session 69600, and proxies `/v1`
to this isolated service. The previous test service/session 16475 was stopped.
The installed app and substrate on port 5377, its credentials, configuration,
and history were left untouched. No installer was deployed. The packaged app
still does not serve a standalone web entry; this proof uses the source frontend.

Verification: Bus browser/transport and documentation suites passed (25 tests);
native suite passed (27 tests); Bus and app builds passed; rebuilt packaged
Command and Extension hosts passed their isolated checks. App verification
also caught an outdated source-boundary assertion from the earlier submission
retry refactor; it now checks the shared submission entry. The app import
smoke test exceeded its timeout during concurrent builds and passed in isolation.
The final complete app suite passed with bounded concurrency: 38 files,
203 passing tests, and 2 existing TODOs. This closes the app regression check,
not the outstanding native, onboarding, model, or end-state acceptance gates.

The previous task, `Simplify floe-app operator entry`, completed its read-only
handover and is idle. This task is the implementation owner. All gates in
`end-state-verification.md` remain open until their own evidence exists.

### Local access correction and first-use participant proof on 2026-09-04

The operator explicitly corrected the connection policy: local access should
require nothing; remote access may require one pairing step per session. This
supersedes the manual local-pairing choice in the earlier F6 attempt.

- **F6 — Local browser access now opens automatically.** The local host enables
  automatic sessions for its configured loopback frontend. The browser receives
  an origin-bound HttpOnly cookie and uses the same Workspace grants, verifier,
  operations and filtered stream. Remote, forwarded, other-origin and cross-site
  requests cannot use this local entry. Remote pairing remains Workspace-bound.
  Multiple local Workspaces have independent credentials. Expired local sessions
  renew on use; stream expiry triggers bounded reconnection rather than silently
  losing updates. PRODUCT.md, ADR-0012 and the first-run guide now state this policy.
- **F7 — Fresh-workspace participant setup repaired.** The conversation resolves
  the authenticated principal's retained Actor binding. First use composes
  `actor.create`, definition publication and `actor.principal.bind` through the
  existing discovered operation contract. It creates no worker Endpoint and
  makes no Bridge-only registration request. Concurrent opens and a lost setup
  response converge on one Actor; revoked access is not restored automatically.
  The default participant definition expresses collaboration and judgement,
  without special rights or a human Actor type. Existing legacy Contexts are
  not reassigned to a new identity by a name or Endpoint suffix; legacy history
  association remains an explicit acceptance concern.
- **F8 — Provider setup.** The first browser attempt reached
  “What do you want to make happen?” with no connected provider/model.
  At that checkpoint model setup pointed to the desktop;
  the outcome composer was disabled and repeated the model instruction. The
  health indicator says the runtime is ready, which does not establish model
  readiness. These are observed first-use limitations, not a successful model
  conversation. The installed older substrate also refused the current native
  broker's provider-account projection; its running process was left intact.

The current proof uses a fresh isolated home
`C:\Users\jfenech\AppData\Local\Temp\floe-astra-operator-oSroSg`, Workspace
`workspace_e33ce422-3a3a-4e6b-bc89-ba8e251a06f9`, backend
`http://127.0.0.1:63068`, and the existing source frontend at
`http://localhost:5379/`. Launcher terminal 45023 keeps its host credential in
memory and accepts `pause` / `resume` for rebuilds. The earlier test home remains
intact; its one-use launcher did not retain a restart credential, so it was not
rekeyed or cleared. The user's installed substrate on port 5377, credentials,
configuration and history remain unchanged. These source/package changes have
not been deployed in a new desktop installer.

Live browser evidence: reloaded the previously disconnected tab and opened the
workspace without clicking a pairing control. After the participant correction,
reload opened the outcome composer directly. The participant proof uses the
same visible frontend and canonical operations, not test-created conversation
results. The first attempt exposed a rejected empty instruction field; the
correction uses a valid definition and keys its exact creation input so a
changed invalid request is not replayed under the previous request's identity.

Restart proof: paused the isolated packaged substrate, rebuilt its helper
package, resumed against the same home and host credential, and reloaded the
same browser tab. It returned to the same outcome composer without pairing.
That checkpoint's isolated backend PID was 106292; the installed backend remains PID 42248.

Current checks: the full app suite passed (39 files, 206 tests, 2 existing TODOs).
Browser connection, transport-boundary and documentation suites passed (28 tests).
Bus and app builds passed. Packaged Command and Extension hosts passed their
isolated runtime checks. The participant integration tests use the real Bus,
canonical operations, persisted binding and Context creation. Native confirmation,
provider setup, real model execution, legacy conversation association, packaged
web serving, and full end-state acceptance remain unproven by this slice.

## Proving sequence

### Real Floe account reuse and first result — 2026-09-04

The operator directed testing to use their installed Floe with a new Workspace
and remove the duplicate substrate. Read-only runtime inspection identified the
installed build as `0.1.32`, revision `f68064445f55`, using `pi-agent-core` on
port 5377. Its account projection already listed ChatGPT and GitHub Copilot.
The newer development client expects different contracts, so a detached checkout
of that exact installed revision supplies the browser client on port 5379.
Only the client runs from `C:\Development\_temp\floe-client-f68064445f55`;
it connects directly to the installed substrate. It has no separate Floe home,
credential store or substrate process. The main development worktree is unchanged.

The Workspace was registered through the actual browser form:

- Name: **Floe operator proof**
- Workspace: `workspace:24baef025c899ea8`
- Folder: `C:\Development\_temp\floe-operator-proof-20260904`
- Context: `ctx_5a78e80a-8370-43ae-93e5-7488dac91537`
- Model: catalog-provided `openai-codex / gpt-5.6-sol`, high effort

Selecting the existing ChatGPT account and model reached Ready without another
provider login. The browser submitted an outcome asking for a one-minute garden
game concept, three distinct plants, one interaction, and a saved Markdown brief.
No account credential was read, copied or supplied by the development agent.

The installed Floe processes stopped during the first attempt. The cause has
not been established; no application crash event or retained log explained it.
The installed app was reopened after both processes and the listener were
confirmed absent. The original message was already durable, so it was not sent
again. The old build records the first Delivery as failed with `delivery lease
expired` and a subsequent Delivery as acknowledged. This observed automatic
retry is not proof of the newer implementation's interruption safety.

The resumed turn completed as `del_52e0d2c6-e656-46b5-b1ba-0a21896094f4`,
with result Event `evt_2793231f-0e77-4a4e-bc73-f4c245a95ce9`. Its `write` tool
created `three-seeds-brief.md` in the requested Workspace. Direct file inspection
confirmed a finished brief covering Sunbutton, Drizzlefern and Rhythmbell, a
hold-for-rain/release-for-sun interaction, a 60-second round and scope guardrails.
Browser inspection confirmed Floe's completion message. This is the first real
model result in this proving sequence; **the account sign-in blocker is resolved**.

**F11 — The result reference is not usable.** The response contains the relative
Markdown link `[Open **three-seeds-brief.md**](three-seeds-brief.md)`. The installed
client renders it as plain text, including the bold markers, with no clickable
link in the accessibility tree. The file exists, but the requested openable
reference was not delivered through the product. Recheck and correct this in the
current implementation before claiming the outcome is fully usable.

Reported telemetry for the completed attempt is 5,970 input and 27 output tokens,
with zero reported cached/reasoning tokens, and approximately 31 seconds from
runtime start to completion. That output count appears to cover only the final
response and cannot establish the total cost of tool-producing model calls.
The full outcome, including the interrupted attempt, is not yet fully metered.

Duplicate cleanup: all four known `floe-astra-operator-*` test homes and the
`floe-astra-operator-proof.mjs` launcher were removed from their active locations
and moved to `C:\Users\jfenech\AppData\Local\Temp\floe-retired-test-setups-20260904`.
Permanent deletion was rejected by automatic approval review with only “blocked
by policy” given as the reason, so cleanup is reversible. No duplicate listener
remains on ports 63068 or 59513. The installed Floe account, original Workspaces
and data were retained. The matching client is PID 89696; after reopening, the
installed app is PID 29980 and its substrate is PID 93424. The browser test tab
is marked to remain available for subsequent work.

### Upgrade rehearsal and F11 correction — 2026-09-04

The smallest observed need is to open a saved result from its conversation.
The current client also lacked an Open action on canonical message attachments.
It now opens the existing exact ArtefactVersion content/history view on demand.
The default Floe instructions direct it to discover publication and Context
attachment capabilities, verify the content, and hand over that reference.
No mutable path is treated as an immutable output reference. The conversation
and exact-version preview tests pass together (25 tests). A real-model repeat
on the updated installed build is still required; F11 remains open.

An offline upgrade rehearsal used a consistent SQLite snapshot of the real
Floe database, without starting another substrate, Bridge or model runtime.
Evidence is in `C:\Development\_temp\floe-upgrade-proof-20260904\report.json`;
the script is the adjacent `floe-upgrade-proof-20260904.ts`.
The current BusStore upgraded schema 0 to 11, creating its own verified backup.
Integrity passed and foreign-key violations were zero. Row-content hashes
matched for all 7 Workspaces, 33 Endpoints, 39 Contexts, 1,292 Events,
699 queue rows, 708 Deliveries, 66,635 telemetry rows and 5 runtime bindings.
Workspace comparison reconstructs the existing local view from canonical
identity plus current locator binding; the first comparator attempt had to be
corrected for that intentional schema change. No live database or credential
was changed by the rehearsal. This is upgrade evidence, not installed recovery
or full portability acceptance.

### Installed update, account reuse and observed cutover faults — 2026-09-04

Floe 0.1.33 was built and installed into the existing `C:\Apps\Floe`, using the
same `C:\Users\jfenech\.floe` home. Before installation, seven installed files
and seven state files were copied and SHA-256 verified under
`C:\Users\jfenech\.floe\recovery\before-0.1.33-20260904`; the database backup
passed integrity and foreign-key checks. The installer left the state files
unchanged. The installed service matched the built bundle exactly. The desktop
executable differed only at the three-byte Tauri bundle marker (`UNK` to `NSS`).
The updated real database reached schema 11 and passed integrity and foreign-key
checks. These checks do not establish a seamless upgrade experience.

Both existing provider accounts were explicitly migrated with discovered
`credential.account.prepare` and `credential.bind` operations through the
packaged native broker. The legacy source fingerprint was checked before and
after; its bytes were preserved. ChatGPT and GitHub Copilot reported connected
at generation 1. No second sign-in or copied test-home credential was used.
The migration report is `account-migration.json` in the recovery folder.

**F12 — Upgrade continuity needs product work.** The new local principal had no
binding to the prior app-created Operator participant, so retained conversations
were absent from the default list. A one-time maintenance action matched six
exact prior app-created Operator records in the verified backup, created and
published their canonical Actor definitions, and bound the authenticated local
principal through supported operations. The script is
`C:\Development\_temp\floe-restore-participant-20260904.ts`; its report is in
the recovery folder. Browser use then reopened the original test Context and
displayed both original messages. No Event or membership was rewritten.
This repairs the known local data; automatic upgrade continuity remains open.
Legacy auth-profile-only model selections also lose their provider in canonical
import. The test Workspace's existing ChatGPT / gpt-5.6-sol / high selection was
restored through the normal model controls; other saved selections still need
their continuity checked and corrected.

**F13 — Valid model selection retained an obsolete missing-reference error.**
The app published a valid model profile but left
`runtime_credential_reference_missing` on its new binding, so it returned to
“Choose an account and model.” Model selection now clears that specific reason
after the verified account reference and grant are installed. The existing
authenticated model-selection integration test reproduces this starting state;
both tests pass. Repeating Use model in the real browser reached Ready.

**F14 — Opened conversations could not send.** The next real send was refused
locally with “Refresh this conversation before sending” because the Context
detail HTTP projection omitted its canonical state revision. The projection now
returns the canonical Context metadata. The authenticated attachment/send test
now obtains its revision from that same HTTP read before invoking communication.
The Context route and server suites pass together (61 tests). Installed 0.1.34
accepted the correction as Event `evt_9f38f3c8-aaf8-4512-a83d-8f1d0a61405a`.
This closes the send preflight failure; that Event then exposed F15.

### Runtime handoff and capability proof — 2026-09-05

**F15 — Saved messages never reached the model.** Three attempts for the
correction expired without being claimed; the last was
`del_29fc1c32-1292-4786-a655-9de82acaf233`. The durable notification omitted its
Workspace, so the authenticated Bridge stream correctly filtered it out.
Adding the nested Delivery to Workspace resolution reproduced a second failure:
runtime preparation rejected the still-reserved pushed bundle. Preparation now
claims an unexpired reservation atomically, respecting ownership and restore
holds. Failed preparation rolls back the claim and emits no claimed notification.
The authenticated push/claim, scoped lifecycle, Context runtime, and lease tests
pass (32 tests), including expiry, wrong owner, rollback, and renewal.

The operator's app was stopped when work resumed. Its installed service was
updated in place, retaining the same home, credentials and database. The
0.1.34 desktop shell and current browser client use port 5377; no duplicate
substrate was started. The first development service hash was
`B90BDA64F8F5502E878ECA0A78D9DB300692C9565D006D3D8D278C10954E5122`.
The old service is retained under
`C:\Users\jfenech\.floe\recovery\before-push-fix-20260905`.

A deliberate continuation through the original conversation created Delivery
`del_21295b03-cb34-4b06-85c9-84822b77bc99` at 23:33:58 UTC on September 4.
It was claimed within 53 ms and injected into the real model. Floe read the
existing brief. This is live proof of F15's handoff correction, not result
completion. The original failed Event remains in history.

**F16 — Runtime operations carried the wrong Delivery identity.** The session
issuer used the transient bundle ID where governance requires stable Delivery
IDs. Actual `artefact.search`, `artefact.create` and `context.get` receipts
were refused with `operation_governance_provenance_invalid`. The issuer now
uses the bundle's pinned stable Delivery IDs. A new authenticated regression
invokes `context.inspect` with the issued runtime token, reproducing the exact
refusal before the fix and completing after it. Governance and runtime checks
pass together (37 tests); live result proof is still required.

**F17 — Obsolete Actor tools undermined the canonical operation path.** After
the refusal Floe tried `create_actor`, which wrote a local Actor file and called
the host-only configuration snapshot route with Bridge credentials. That route
refused the request, after which runtime telemetry stopped. The UI remained
“Floe is working.” Actor file shortcuts have been removed from the runtime tool
catalogue; Actor management uses discovered semantic operations and active
Delivery authority. The unused implementation and its legacy-contract tests
were removed. Runtime catalogue, capability, daemon and adapter checks pass
(92 tests); Bridge type checking and packaged host checks pass.

The brief remains exactly 2,447 bytes with SHA-256
`f138762a4c6f0b374c1e5f966a7793c61d19a9ccc950dc50f8fdda050d6117bb`.
No ArtefactVersion was created by the failed attempt. Its generated
`.floe/agents/artefact-publisher.md` and configuration entry remain evidence;
they were not promoted into a verified collaborator or result.

The next installed development service hash is
`BBFEDA4CE059D806C83F4CA6F1EAB66A6CB7AC974E3ACFEC86063213C0C63BB0`.
The desktop was closed and the stuck service stopped to apply it. The previous
service is retained as `floe-desktop-push-only.js` in the recovery folder.
This is a development update of the real installation, not a new installer
release or proof of clean installation.

**F18 — Direct conversation recovery is incomplete.** The stalled direct
conversation offered no Stop control. Its earlier terminal message said to
check Settings even though the cause was a delivery defect. Accurate failure
feedback, cancellation and a supported continuation path still require product
work and live proof. A service restart is recorded as a development intervention,
not successful operator recovery.

Restart did produce a durable interruption report, Event
`evt_aeeaad95-7234-4467-b61c-7719c1113168`, and settled the interrupted Delivery
without automatic retry. That specific recovery behaviour is verified. It does
not replace the missing Stop control or a complete operator recovery experience.

**F19 — File tools depended on imported Actor files.** The next real turn
(`del_fbd7ec24-e8c1-492c-af34-3111cc2d38e8`) completed permitted operations,
confirming F16 in live use, but lacked file tools after a guarded configuration
import refused the changed file inventory. The runtime took its locator from
that import's Actor cache instead of the accessible, host-bound Workspace. Floe
requested the file hash from the operator. Runtime file access now follows the
authenticated Workspace binding independently of Actor source files. Removed,
uninitialised or inaccessible bindings clear cached access. Pinned-runtime tests
cover both an available locator and its absence. Bridge suite: 367 tests pass.

The installed development service with hash
`1BDDC062DFB309F956E6FE4531F94FE0C4FE79F9C2821B71BB7973DA605C5D50`
ran the next continuation as `del_4bb18a79-8403-46bc-9ab0-dc0f3fbc1002`.
Floe read and hashed the file itself, published correct versions and verified
their content with `read_artefact`. The source hash and 2,447-byte size remain
unchanged. F19 is verified for this real file task.

**F20 — Publication trusted unverified local content identity.** Without file
tools, Floe probed publication with an all-zero digest. The operation incorrectly
completed and retained `artefact_version_6a03eaba-f486-462b-9cc3-be5951b7a3f3`.
Reading such a version refuses mismatched content, but publication itself did
not verify or retain bytes. The canonical publication operation now uses the
Bus content owner to read and verify the local file, retain its bytes under the
existing `.floe/content/sha256/` store, and publish that retained reference.
The metadata store remains separate from local content storage; attachment
ingress already commits exact bytes through the same content store.

An authenticated regression reproduced acceptance of the false digest before
the correction. It now proves refusal without a version, exact original content
after a later source edit, receipt replay after that edit, and removal of new
content when a deferred database failure rolls back publication. Resolver,
publication and Context checks pass (27 tests); Bus type checking passes.
Live publication on the packaged correction remains required. Prior versions
are retained as evidence, including the bad probe; none is silently rewritten.

**F21 — Canonical attachments were not rendered without display metadata.**
The next turn attached its correct version to an Event through canonical
association, but the app read only `content.attachments`. The message stayed
plain text despite valid Event ArtefactVersion references. Rendering now starts
from those canonical references, uses matching display metadata when present,
and otherwise offers “Open Saved result.” Unbound display claims are ignored.
The runtime `emit` tool now accepts exact ArtefactVersion IDs explicitly, so
attachment references need not be hidden in arbitrary `data` fields.

The browser opened `artefact_version_51e82baa-f167-4b62-9c2b-38119f0c950b`,
attached to Event `evt_aabbb38b-9ff9-4c4f-8d2f-84ddf28b4b1f`, and displayed the
full Three Seeds brief, exact digest and size. This closes F11's missing Open
path for the current client and establishes a real model-produced, readable
result. It does not establish retained-byte publication for that older version.
Attachment rendering and conversation checks pass (28 tests); runtime adapter
checks pass (63 tests), including explicit exact attachment forwarding.

**F22 — Reading comfort remains below the product contract.** A screenshot of
the opened result shows a small reading area beneath technical metadata and
nested scrolling. Markdown is displayed as source. The result can be read, but
the default reading experience still needs a focused client correction and
visual verification. Keep version diagnostics accessible through disclosure.

**F23 — Complete outcome usage remains unmeasured.** The adapter records usage
from the final assistant message. That does not include every tool-producing
model response or a turn interrupted before finalisation. Do not present those
figures as total outcome cost. Complete per-response accounting and repeatable
outcome measurements remain required before claiming token efficiency.

The complete Bus suite including F20 passed: 114 files, 840 passed and one
skipped. The Bridge suite passed 367 tests before the final emit attachment
change; its adapter suite then passed 63 tests. App attachment/conversation and
exact-version preview checks passed 30 tests. Documentation checks passed nine.

Installed 0.1.35 through its generated NSIS installer. The installed service
matches the built bundle with SHA-256
`7F5985946CDEF8DA12F1B62E7EADC6D6712F87536D85702CF158257874473AEA`.
Database quick-check is `ok`, with zero foreign-key violations. Desktop PID
77000 owns packaged service PID 63240. Browser access and the existing
conversation returned without pairing or account setup. Native installer UI
and native-only interaction remain outside the available computer-use surface.

The two completed model attempts took about 122 seconds and 120 seconds,
using 15 and 16 tools respectively. Their final-response usage reports are
136,837 and 52,648 tokens, not complete turn totals. The interrupted attempt
used 11 tools and has no final usage record. The repeated full schema discovery
responses are a concrete context-efficiency concern to investigate alongside
F23. Development restarts and continuation messages are interventions; they
are not evidence of autonomous recovery or full product completion.

### Exact message attachments and reading proof — 2026-09-05

The real 0.1.35 turn for Event `evt_82a07530-b747-44b6-bd5e-ab740039c75d`
published version `artefact_version_8d72351a-b173-4388-b383-28f879c63aaa`.
Its verified 2,447 bytes were retained at `.floe/content/sha256/` under digest
`f138762a4c6f0b374c1e5f966a7793c61d19a9ccc950dc50f8fdda050d6117bb`.
The working brief stayed unchanged and the browser opened the complete version.
This proves F20's retained publication in the installed service. The attachment
appeared on the operator's request instead of the model's reply, exposing two
further faults; the saved bytes alone did not complete the original experience.

**F24 — Direct emission discarded exact version references.** The runtime tool
sent `artefact_version_ids`, but the HTTP schema stripped the field and
`submitEvent` did not forward it. Both now pass the reference to canonical Event
insertion. The authenticated HTTP regression reproduced the empty attachment
list before correction, then exposed F25 after the field was restored.

**F25 — Later relationships changed earlier message attachments.** Event reads
derived references from every Artefact association, including later outputs.
Adding an association could change a message's envelope and its next delivery.
Events now store their reference list at insertion. Later associations remain
inspectable relationships. Tests verify identical emission, read, idempotent
replay and delivery after new output and attachment associations are added.

Schema 12 preserves the previous visible attachment projection once and marks
old Events with `metadata.artefact_reference_basis = legacy_projection_at_schema_12`.
It does not claim to recover the original emitted inputs or rewrite delivered
envelopes. Migration/restart tests verify the snapshot stays fixed, the original
content and metadata remain, and a verified schema-11 recovery copy exists.
An offline rehearsal against the real database checked all 1,304 Events: no
content or existing metadata changed; visible references were preserved; integrity
passed with zero foreign-key violations. No second substrate was started.
Evidence: `C:\Development\_temp\floe-event-history-upgrade-20260905\report.json`.

**F22 — Reading correction verified in the browser.** The same retained brief
now renders Markdown at readable size without its own nested scrolling region.
Content follows a short saved-version label; technical facts and related records
sit behind Version details. The browser screenshot and accessibility tree show
the complete brief with those details collapsed. This is a focused improvement
to the current reading surface, not completion of the full product UX.

The Bus suite passed 837 tests with one skipped; five portability fixtures failed
because they still declared schema 11. After updating those fixtures to the current
schema and stored Event references, all five passed. Combined: 842 passed, one
skipped across 115 files. App preview/conversation checks passed 26 tests.
Bus type checking and nine documentation checks passed.

Installed 0.1.36 through its generated NSIS installer. Desktop PID 45576 owns the
only installed substrate, PID 29108 on 5377; the browser client is PID 11492 on
5379. The installed service matches the build with SHA-256
`8F620A540197BA2F11AA58013432211372C340F15286600281A65985144F9BA7`.
Schema 12 passed quick-check with zero foreign-key errors. The automatic recovery
copy passed integrity checking and retains all 1,304 pre-upgrade Events. The previous
installation was copied to
`C:\Users\jfenech\.floe\recovery\before-0.1.36-20260905\installation`;
its service hash matches the previously verified 0.1.35 bundle. Both existing
provider accounts remain connected. The retired duplicate homes remain archived,
with no listeners on their prior ports.

**Installed selective-correction proof:** The operator prompt asked only to change
Drizzlefern's colour from soft blue to copper-orange, retaining all other content.
Request Event `evt_109f84c8-d65e-442c-8414-ef8336f3f0fd` led to Delivery
`del_a97f158d-d59c-4cd8-b5fb-5ca9beb49fb6` and RuntimeTurn
`rt_f57877ba-f709-4e13-8c13-d9290f7f0b49`. It completed and was acknowledged.
The only file difference is `a soft blue fern` becoming `a copper-orange fern`.
The model published version `artefact_version_67497087-c366-45c8-a95f-06b3ee50a828`
(ordinal 5), with 2,451 retained bytes and verified digest
`c6b8b1dc985b58c0242ccab747482579dfc6167f5d3d7f119806647d19aa379d`.
Reply Event `evt_8f00493a-67bb-45cf-87de-b7839d64fc20` carries that exact version;
the new operator request still has no attachments. The browser opened the revised
brief from the reply, loaded version history, selected version 4, and showed the
original soft-blue wording after the working file had changed. No login, manual
publication, maintenance or repair message was required during this attempt.

This closes the installed repeat for F24/F25 and establishes one real retained-byte
selective correction. It does not prove all content types, approval invalidation,
stopping, collaboration, portability, native UI or the campaign journey.
Evidence: `C:\Development\_temp\floe-upgrade-proof-20260904\selective-revision-0.1.36.json`.
Elapsed time was about 86 seconds with 15 tools. Final-response usage was 35,452
tokens; full-turn usage remains unavailable. F23 is the next observed obstacle.

### F26 interactive saved result — 2026-09-05

The operator attempt asked Floe to turn the existing Three Seeds brief into a
playable browser game, make its own creative and implementation choices, test
what it could, and return a saved result openable from the conversation. All work
was constrained to the test Workspace, with no external publication and no change
to the brief. Request `evt_94d1e8e2-c2fc-49b1-9be1-73573b158051` produced acknowledged
Delivery `del_01ae99a4-53c9-4494-890a-625d8468659e` on installed 0.1.39.

Floe wrote the HTML/CSS/JavaScript, model tests and bundling check itself, then
published `three-seeds-game`, type `interactive.web-game`, as exact version
`artefact_version_fd01e408-aaa0-47d6-bd3f-e8fd2b43cce5`. The self-contained result
`three-seeds-game/three-seeds.html` is 25,160 bytes, SHA-256
`6f1510bfd44020159ea5c226edd780f950c2d037b131a434a8977172edcceead`.
The development agent did not create or edit this result. The brief retains digest
`c826a4ef8ae3e43c7fea28572fd44172a7fe1d9c5dffac82a4ff0efd300a1880`.

**Observed failure:** Floe claimed the game was ready to open, but the attachment
showed `No inline preview for application/octet-stream`. Canonical content was
retained correctly; the client and MIME transport lacked an interactive HTML
representation. This is F26, a client/content-transport correction, not a new
substrate primitive or Extension framework.

The correction keeps the normal exact-version reader and grants. Opening HTML
offers Run; executing it uses an opaque sandbox with no Floe account, Workspace
or native bridge access. A static public bootstrap receives verified bytes once.
The content endpoint itself forces attachment disposition and disables execution
in its authenticated origin. Run discloses that browsers still permit some internet
access; this is not an offline sandbox. The browser regression proved WebRTC STUN
requests despite `connect-src 'none'`, so no network-free claim is made. See
`html-preview-boundary.md` for the exact contract and reproducible security checks.

Installed 0.1.40 first proved that the exact saved HTML loaded. Actual use then
exposed two client problems: the conversation cropped the game, and keyboard
input could remain on the Run/Stop button. The final candidate opens a larger
modal and focuses the game. A real keyboard round, alternating 1.8-second rain
and sun intervals, reached **All three in bloom** with six seconds remaining.
The screenshot shows ivory Sunbutton, copper-orange Drizzlefern and sea-green
Rhythmbell. Play Again returned to 60 seconds. Escape from inside the game exposed
a missing exit path; the bootstrap now has one reverse message that only closes
its own preview. The winning round used the candidate client and installed 0.1.40
service before that final Escape change.

Installed **0.1.41** then passed the full repeated exit path: Run opened the saved
game, Escape removed the iframe, Run started fresh, and Stop removed it again.
A pointer-started round left in sun ended at zero seconds with one of three plants
in bloom; Play Again restored 60 seconds and Escape returned to the conversation.
The final browser check used the real installed service, not a test content server.
The native Windows window itself remains untested by the available computer tools.

The 0.1.41 installer and client build passed. The installed service SHA-256 is
`758fc070559def48b0fcfbcca3eb44128bf7b319145f00f6c6f42399a576a033`, matching
the built resource. Recovery copies before 0.1.40 and 0.1.41 preserve the previous
installation, configuration, accounts and a checked 1,320-Event database. The
final live database passes integrity checking, retains all 1,320 Events, and has
no active Deliveries. The exact retained game, its working source and the brief
all preserve their recorded hashes. No new model request was needed to make the
saved result playable.

Proof files under `C:\Development\_temp\floe-upgrade-proof-20260904`:
`game-attempt-del_01ae99a4-53c9-4494-890a-625d8468659e.json`,
`html-browser-security.json`, `html-preview-game-win.png`,
`html-preview-game-timeout-0.1.41.png`, `html-preview-installed-0.1.41.json`, and
the HTML test and installer logs.
The latest regression results are 17 app tests and 6 content-server tests, with
the earlier 20-test resolver/transport run also passing. The browser regression
uses a static fixture without a Floe home or credentials; it complements the real
game and is not counted as model-produced work or native-window proof.

Complete game-generation accounting: 23 model responses, 31 tools, approximately
347 seconds; 274,296 input, 14,115 output, 145,536 cache-read, zero cache-write and
433,947 total reported tokens. The 2,390 reasoning tokens are already included,
not added again. The source tests and successful conversation alone did not
establish playability: F26 repair and the actual controls/playtest were necessary.
This single creative outcome is not an efficiency benchmark or proof of the
remaining content-type, Extension, mobile, collaboration or campaign gates.

### F27 independently inspectable 3D results — 2026-09-05

The operator attempt used **New with Floe** in the real installed 0.1.41 app and
the existing test Workspace/account. It requested a Moonlit Courtyard with a
wooden bench, terracotta planter and lantern; independently inspectable objects,
an assembled scene that can be rotated, and later selective correction. Floe
was asked to make its own implementation choices, keep work in a new folder,
preserve Three Seeds and avoid external publishing. No developer-authored actors,
scene files, routing or results were inserted into the attempt.

- Context: `ctx_fe7718fb-f88f-4b3a-b0b5-9a5126e9db13`.
- Request: `evt_bc871e4a-0bb7-446e-8758-141d5d2aab7c`.
- Acknowledged Delivery: `del_48846afb-6fbe-4341-b244-b07714614fe1`.
- RuntimeTurn: `rt_c6727e4b-78e4-4e8d-b378-5ca8eee14476`.
- Attached result Event: `evt_113a265f-c3e2-486b-9cd5-6b04c4fc12b4`.

Floe generated and structurally validated four glTF 2 binary files, a deterministic
Python generator, validator, README and manifest. The creation took about 362 seconds
and 23 reported model responses: 281,249 input, 14,756 output, 196,096 cache-read,
zero cache-write, 492,101 total tokens. The reported 3,210 reasoning tokens are
included in output, not added again. This measures creation, not a successful
complete review/correction outcome.

The canonical records, rather than only the model's manifest, prove:

| Result | Exact version | Stable Artefact |
| --- | --- | --- |
| Scene | `artefact_version_8da8cca3-0064-4341-92e0-2d017e473456` | `artefact_658027e3-d5a4-433c-8744-552a2bafef66` |
| Bench | `artefact_version_135c96e0-18ff-4d26-8b31-c843c465a207` | `artefact_51b564b6-a5e3-451e-8b37-f2c165e6707d` |
| Planter | `artefact_version_a57af497-a244-4f7b-9e3e-e9266effe05e` | `artefact_0f94011b-b1b2-4cde-a2c0-3553b354135b` |
| Lantern | `artefact_version_0e3369c5-1e92-4f89-ad5a-236a447ed412` | `artefact_a12b2005-d9fb-4b08-9b1c-e4489dc88a6b` |

The scene records three `core:derived-from` relationships and keyed collection
members (`wooden-bench`, `terracotta-planter`, `lantern`) pointing to those exact
component versions. All four retained content digests match their bytes. The eight
scene source files and all original game files/brief remained unchanged during
developer inspection. Database integrity is `ok`.

**F27 observed:** opening the first attachment through the app displayed
`No inline preview for application/octet-stream`. Floe's claim that the scene
was ready for interactive review was premature. A saved file plus structural
validation does not prove that the operator can inspect it.

The correction is client representation and content transport: preserve GLB MIME
identity and use a locally bundled standard viewer inside the existing opaque
preview. It adds no scene primitive, executable Extension registration or new
source of canonical state. See `html-preview-boundary.md` for the boundary and
reproduction commands. Before installation, nine app tests, ten content tests,
type checking, the existing HTML browser security regression and the new model
browser regression passed. The latter opened all four actual files in a static
fixture, proved rotate/reset and Escape, embedded texture rendering, inert model
markup and refusal of outside resources/malformed files. This fixture is not
installed-app operator proof.

Evidence is retained under `C:\Development\_temp\floe-upgrade-proof-20260904`:
`scene-baseline-del_48846afb-6fbe-4341-b244-b07714614fe1.json`,
`scene-integrity-0.1.41.json`, `model-preview-browser.json`, and
`html-browser-security-with-models.log`.

**F28 observed:** all four references are labelled Saved result 1–4. Existing
conversation attachment metadata supports names, but the runtime's `emit`
wrapper exposes only exact version IDs and puts optional structured data beneath
`content.data`; the client reads names from `content.attachments`. Inspect the
canonical communication operation and choose an actor/tool-contract correction
before adding any substrate identity metadata. This remains open.

Installed **0.1.42** now passes the original opening attempt. The signed-in browser
client used the installed service; no content response or operation was mocked.
The assembled scene rendered its courtyard, bench, planter and lantern. Pointer
drag changed the scene view, wheel zoom changed framing, Reset view returned the
camera and Escape closed the preview. Each original component attachment opened
separately, rendered the expected object, rotated and closed through Stop preview.
Screenshots `model-scene-0.1.42.png`, `model-scene-rotated-0.1.42.png`,
`model-bench-0.1.42.png`, `model-planter-0.1.42.png` and
`model-lantern-0.1.42.png` were inspected. This is browser proof; native Windows
window and mobile interactions remain untested.

The installer recovery directory is
`C:\Users\jfenech\.floe\recovery\before-0.1.42-20260905`, with 1,323 Events in
the integrity-checked backup. Installed service SHA-256 is
`4ba739355c66404b324eb2ed5771b9923e2320ce68bafef1f71f1729a9a9cd67`.
Post-installation `scene-integrity-0.1.42.json` confirms all eight scene files and
all four retained versions unchanged, with the exact lineage/membership intact.

A new message in the same real conversation requested only a teal metal finish
on the lantern frame, roof and handle, preserving warm glow, all other objects,
positions, earlier versions and Three Seeds. It asks for the revised lantern and
assembled scene as clearly named attachments.

**F29 observed:** correction Delivery
`del_14a5c4db-0337-442e-abb8-3b0ee3668649` (request
`evt_29a78639-d21a-44b9-ab06-8d89eb57858e`, 03:06:14 UTC) stopped producing
telemetry after an edit completed at 03:06:49. Its files changed at 03:06:59,
but the UI continued to say Floe is working and no new result or runtime error
arrived. No command process remained. After more than seven minutes without
new progress evidence, the developer used **Stop Floe response**. The Delivery
became cancelled and the UI stated that existing changes remain. The cause is
unresolved; absence of telemetry does not prove a provider failure.

The stopped attempt reported only five responses and 65,512 tokens (51,544
input, 912 output, 13,056 cached input, 249 reasoning included in output).
There is no final coverage record, so these are partial measurements, not the
complete cost. Evidence: `scene-stopped-del_14a5c4db-0337-442e-abb8-3b0ee3668649.json`.
Read-only comparison of the current files against the retained originals found
identical binary geometry and node records; only the lantern metal material
changed in both the standalone lantern and scene. Those files were preserved.
An explicit new conversation message asks Floe to reuse them, verify the edit,
retain new versions under the original result identities and finish the named
attachment handoff. Recovery and selective-change success remain unproven.

The first recovery request (`evt_4be0a93e-d9b4-432c-9342-75602e034946`) never
reached the model. Delivery `del_fd3c7e4d-79e7-4269-9928-d67663c601b3` and its
successor `del_04810fee-6685-478c-b066-4d679636eb67` expired; the latter was
dead-lettered. The UI showed that the message could not be completed. The earlier
inference that cancellation had left the model running was incorrect.

The installed log at
`C:\Users\jfenech\AppData\Local\com.floe.console\logs\Floe.log` establishes
the cause. At 03:06:57, watcher
`graph_dbd1083c-0d1a-45ce-a493-6ddea9d529cd:folder-arrival` called
`fireScopeGraphTriggerNode` with Bridge-service credentials. That route requires
Workspace-operation credentials and returns 401 for the wrong audience. The
watcher belongs to Scope `concept-prop-extraction` in another Workspace,
`workspace:bb250e88b1cf5bdf`, so its refusal interrupted unrelated work. The
client interpreted every 401 as global credential invalidity and discarded the
valid Bridge credential. Telemetry then failed at 03:06:58; the adapter failed
at 03:06:59, and could no longer report that failure to the Bus. Its socket stayed
connected until later traffic, leaving the misleading working/healthy projection.
This was not a provider-thinking stall or actual credential expiration.

The bounded client correction rechecks a rejected credential once through the
existing Bridge-only Workspace-binding read. A 401 there invalidates the
credential; a wrong-audience refusal elsewhere remains an operation failure.
A probe network failure cannot establish credential invalidity. No request is
retried with broader authority, no audience is interchanged, and no recurring
liveness loop is added. The legacy watcher operation remains refused; this
repair does not prove or authorise legacy source execution. Canonical folder-source
acceptance remains part of the pending full journey.

Twenty-six Bridge client regressions and Bridge type checking pass. They cover
continued authorised work after the watcher refusal, true credential rejection
without recursive probing, and an interrupted probe. An authenticated local HTTP
regression uses the actual Bus and Bridge client, preserving the refused route
while successfully listing bindings and reporting liveness with the same original
credential. Installed recovery and the saved-result handoff are the remaining
checks for this increment.

Installed **0.1.43** includes that correction. Its integrity-checked recovery
copy (`C:\Users\jfenech\.floe\recovery\before-0.1.43-20260905`) contains
1,326 Events. Installed service SHA-256 is
`b0840f16c126b3fccf8be5360ebd470a998457fe470a84df9ee2fc91a21ee7a2`.
The authenticated Bus/Bridge integration suite passed all 12 tests, and the
packaged build completed. The same installed Floe restarted with the existing
accounts and test Workspace. A new operator message asks for the preserved
result handoff; it does not authorise regenerating correct work or publishing
externally. Live completion remains pending.

**F30 observed:** the post-install retry is retained as
`evt_20e16a65-636b-4e33-ad5a-f2c7a0432cec` at 03:33:28 UTC, but no Delivery was
created. The actual browser shows the message, model Ready, and global Floe
running, with neither working progress nor an explanation for this message.
The supported config-status API reports this Workspace as `config_invalid`.
The importer replays refusal
`workspace_config_import_560ba8de01b333ef4dcccf839cb4fead` from 23:41:59 UTC on
September 4: `workspace_configuration_conflict`, because canonical Actor or
Runtime state changed outside the import boundary. Its source config hash is
`sha256:90bec942b0810a1cefb2c4faae4c711f24864c84d7593f64f2916b615a3f81c4`.

The Endpoint metadata's missing-credential reason belongs to the older import
binding and is stale; it does not establish loss of the current account.
`FloeModelControl` loads the current canonical binding and only says Ready when
that binding is resolved and the account is connected. Startup
`BridgeDaemon.attachWorkspace` instead returns immediately when import is
refused, before populating its runtime registrations. This separates a valid
saved model configuration from an executable attached runtime, while the UI
does not explain the distinction.

F30 correction separates file import from runtime attachment. The Bridge reads
current published Actor definitions and retained runtime bindings through a
Bridge-only projection bound to the current Workspace locator. Refused imports
remain evidence; neither their Actor snapshots nor source-file instructions
become runtime configuration. Unimported files cannot start new legacy Event
sources. Disabled bindings and retired Actors, Endpoints or Runtime Profiles
are excluded. Actual runtime instructions, model and authority still come from
the Delivery's immutable processing contract.

An integration test changes the saved instructions and model after import,
retains and replays a refused import, queues a request, then attaches a fresh
Bridge over actual HTTP. The request is delivered with the exact saved revision
and binding IDs. The same test passes with invalid source YAML. Missing or wrong
transport authority and stale locator bindings are refused; disabled bindings
and retired Endpoints do not attach. Thirty focused Bus checks and 53 Bridge
checks pass; both production TypeScript builds pass. The Bus production build
now excludes test entry points so cross-package integration tests do not emit
JavaScript into the Bridge source tree. The accidental build emissions were
identified by their exact timestamp and corresponding source files and removed.

Version **0.1.44 is installed**. Its service hash matches the packaged build:
`72c738ec72a7e2b51441c07d4f323a5cae0281474f1bc4607aa7417ae10be01e`.
The integrity-checked recovery copy is
`C:\Users\jfenech\.floe\recovery\before-0.1.44-20260905`, with 1,327 Events.
Before/after evidence in `restart-before-0.1.44.json` and
`restart-after-0.1.44.json` verifies identical Actor definition and runtime
binding IDs, model, instruction digest and configuration-file digests. The
config-status API now reports `attached`. The previously waiting request
created exactly one Delivery, `del_ea618195-d0bc-4101-a290-3628575bd9cf`, pinned
to those saved records, and entered the model runtime. No message was resent.
The actual browser showed working progress and a Stop control. The Delivery
completed and was acknowledged at the end of the response; the full goal remains
active.

### Selective scene correction completed on 0.1.44

The resumed response attached version 2 of the existing lantern
(`artefact_version_ebf793a5-dc62-4346-9dd5-a4f29679ef22`) and assembled scene
(`artefact_version_e0860f52-878a-4604-8a04-fe27dfaed80a`) in Event
`evt_eed0b26e-8588-4e0d-a9b8-7d4ab681f178`. Read-only verification compared the
retained original and revised GLBs: their binary geometry and node transforms
are identical, and the complete JSON diff consists only of the lantern metal
material's name, colour, metallic factor and roughness. Warm glow is unchanged.
Bench and planter keep their original version IDs and digests. New scene
membership references those exact originals plus the revised lantern; original
scene membership is unchanged. Three Seeds game and brief hashes also match.
Floe authored its verification script; the development agent did not edit the
scene assets.

The actual browser opened the revised lantern and scene, showed the teal finish
and warm glow, rotated the scene, reset it and closed with Escape. The original
lantern remains openable after the correction. Evidence includes
`scene-selective-correction.json`,
`scene-corrected-del_ea618195-d0bc-4101-a290-3628575bd9cf.json`, and screenshots
`teal-lantern-0.1.44.png`, `teal-courtyard-0.1.44.png`,
`teal-courtyard-rotated-0.1.44.png` and
`original-lantern-after-correction-0.1.44.png` in the external proof folder.
This is browser proof against the installed backend, not native-window or mobile
interaction proof.

**F28 remains observed:** Floe supplied clear names in the message text, but its
attachment buttons still read Saved result 1 and Saved result 2. The Emit tool
exposes exact version IDs but cannot supply the existing client contract's
`content.attachments` display metadata. Diagnose that tool-contract gap using
the existing attachment representation, preserve immutable Event membership,
and repeat the handoff without regenerating or republishing the models.

**F31 observed:** the successful recovery used 32 measured model responses over
approximately 278 seconds: 574,936 input, 8,290 output and 241,536 cached-read
tokens, totalling **824,762**. Its 2,170 reasoning tokens are included in output.
The earlier stopped attempt contributes another 65,512 measured tokens with
incomplete coverage; the combined known count is at least 890,274 and is not a
complete outcome cost. The successful response performed repeated capability
discovery and checked Context history after emitting. The Emit wrapper discards
the Bus's accepted Event reference and returns only `emit accepted`; inspect
that lost confirmation alongside the measured discovery sequence before choosing
the smallest correction. Do not claim an efficiency improvement without a
matched useful outcome and complete accounting.

F28 installed correction: the Emit tool now accepts named exact-version
attachments and maps them to the existing `content.attachments` display
metadata and canonical Event version references. Named versions need not be
repeated in a second input array. No client renderer, new identity record or
mutable attachment membership is introduced. The Bridge also returns the Bus's
accepted Event ID, time and exact version references to the model instead of
discarding that confirmation. This addresses an observed reason for redundant
post-send history reads; it is not yet a measured efficiency improvement.

The 106 focused Bridge checks pass, including distinct display names, unique
canonical version references and confirmation taken from the retained Bus
response rather than echoed request input. Four client attachment checks and
TypeScript also pass. Version **0.1.45 is installed** with service hash
`364895412fc3a4ee650e6a3fe51d3a7ef44ecf86f0e7b40cc90de05dbee75f0b`.
The integrity-checked recovery copy
`C:\Users\jfenech\.floe\recovery\before-0.1.45-20260905` contains 1,329 Events.
The first packaging attempt accidentally changed the unrelated
`tracing` lockfile version during release stamping; it was restored to 0.1.44
and the build restarted only after the original process exited with an error.
Future release stamping must target the Floe package entry specifically.

The actual browser submitted Event `evt_973906ef-9bc1-4788-a643-506574a642ad`
requesting the existing revised models with buttons named Teal Lantern and
Moonlit Courtyard. Delivery `del_b302dd50-e9a1-4c95-927f-09cc8629a020` completed
and was acknowledged. Its named attachment Event is
`evt_bc5ccdfa-58ce-4b7a-ba2d-03abc14f4aea`. Both named buttons open their correct
3D models in the real browser. Before/after reports
`named-handoff-before-0.1.45.json` and `named-handoff-after-0.1.45.json` verify
identical 11 file digests and all 14 existing Workspace versions; no model was
edited or republished. Screenshots `named-attachments-0.1.45.png` and
`named-courtyard-0.1.45.png` retain the operator view.

This handoff used three measured model responses and approximately 17 seconds:
19,946 input, 221 output and no cached tokens, totalling **20,167**; 39 reasoning
tokens are included in output. The only tools were one initial Context history
read and Emit. There was no post-send history lookup or capability discovery.
This establishes useful behaviour for the narrow handoff, not a matched
comparison with the earlier verification-and-publication recovery. F31 still
requires broader successful-outcome efficiency evidence. Full accounting is
retained in `named-handoff-del_b302dd50-e9a1-4c95-927f-09cc8629a020.json`.

Further F31 inspection shows the repeated exact-operation discovery calls had
different Artefact targets. Two explicitly requested result schemas; discovery
does not inject those schemas by default. Do not treat every repeated operation
name as unnecessary discovery or suppress target-specific authority evidence.

### Independent Actor collaboration attempt — 0.1.45

The next actual New with Floe outcome requests an independent AI collaborator
to review Three Seeds, identify the most useful improvement for a first-time
player with evidence, and bring back their review plus Floe's recommendation as
a named saved result. Floe is to organise the collaboration using available
accounts, preserve the existing game, brief, courtyard and saved versions, and
make no external publication. No Actor topology or pipeline was authored by the
development agent.

Request `evt_7243f392-7e32-48ed-a446-70ed014a5425` created Context
`ctx_aaa95cce-7d57-4fe3-b43c-61946877721e` and Delivery
`del_62821d21-7f29-4855-a67f-ef5597cf1a5d`. The response was stopped through the
actual Stop control after a runtime attachment failure was established. External helper
`floe-collaboration-proof-20260905.mjs` records canonical Actors, bindings,
Deliveries, public results, per-response usage and preservation checks in
`collaboration-observe.json`. This attempt has not yet proved independent work
or a completed review.

**F32 — A newly configured collaborator never attaches to the running Bridge.**
Floe created two published Actors and retained resolved runtime bindings, but
neither had an Endpoint. The current binding callback only updated already
attached Endpoints; it never notified the Bridge to discover a newly bound Actor.
The stopped Delivery ended with 48 measured responses: 737,016 input, 6,448
output, 862,592 cached input, and 1,606,056 total tokens. Reasoning coverage is
incomplete. This is failed-outcome cost, not useful collaboration. The original
11 file hashes and 14 saved versions remain unchanged. Preserve both Actors;
the second acquired an Endpoint ID before cancellation, but no live attachment.

The new real HTTP/WebSocket regression reproduced the absent Endpoint after
Bridge startup. The correction pushes a minimal Workspace-bound notification
for the retained current binding after the enclosing synchronous transaction
settles. Rolled-back and superseded bindings cannot announce attachment. The
Bridge uses canonical runtime discovery and coalesces notifications arriving
during an attachment pass. No polling, runtime grant copying, or imported-file
authority was introduced. The test also exposed rediscovery resetting unrelated
waiting work; attachment refresh now preserves existing active, waiting, error
and retired states on the same Bridge. Explicit work transitions retain control
of continuation and recovery.

**F31 — Discovery target guessing.** Search summaries now include the operation's
existing target contract. The optional target filter explains that discovery can
omit it until the resource kind is known. Exact lookup still returns the same
target-specific authority decision and input schema; result schemas remain
opt-in. This addresses the observed guesses at actor-definition resource kinds
without preloading all operation schemas or weakening authority.

Source checks pass: 131 Bus tests, 34 Bridge tests, including live attachment,
rollback, attachment races, preservation and bounded discovery. Bus and Bridge
TypeScript builds pass. The packaged Command and Extension checks and desktop
build pass. Changes in this increment pass whitespace checks; the wider dirty
worktree retains two earlier blank-line-at-EOF findings in FloeModelControl files.

### Installed collaboration continuation — 0.1.46

Installed 0.1.46 after verifying no active Delivery and a recovery database with
1,334 Events and successful integrity check. Recovery directory:
`C:\Users\jfenech\.floe\recovery\before-0.1.46-20260905`.
Installed service SHA-256:
`c4e5cb2850da5e03b8e91091f89ba9cc63f421277eab60a8fc8d03e55bbc0be6`.
Desktop PID 63116 owns substrate PID 87716 on 5377. The browser on 5379 connects
to this installed service; no duplicate substrate listener exists. Account and
model choices remain usable without a sign-in, and both saved reviewers now have
attached Endpoints and their published names in the conversation.

The same conversation was reopened through its Recent card. Continuation Event
`evt_f288dfe1-c589-4b56-8373-3f08ad2f76c4` asks to use an existing collaborator,
preserve the existing work, and report any exact access refusal without repeated
setup. Floe's Delivery `del_c43428f9-dde4-4979-888f-1ae33378b66c` acknowledged
after requesting the reviewer with the exact saved game and brief references.
Reviewer Delivery `del_7659f01b-13f5-4def-9a90-822305eb725c` was created and
reached runtime preparation, establishing progress beyond absent attachment.
The independent model did not run.

**F33 — Collaborator permission references are accepted for the wrong principal.**
Runtime preparation correctly refused the reviewer's two grants: both are
active, but belong to the Floe Actor, not the reviewer. The other new reviewer
has the same copied references. Actor definition publication and the resolved
binding had accepted this invalid combination. Do not copy or reassign the
Floe grants, remove the principal check, or count a connected Endpoint as an
authorised collaborator. The generic need is a supported way to give a
collaborator its own bounded authority, with invalid references explained before
the runtime attempts work. Inspect existing grant issuers, semantic operations,
and explicit product policy before adding machinery. A capability to perform
work is not automatically permission to grant it to someone else.

Floe received the durable failure through the normal request response and
completed follow-up `del_30e6222b-978e-4e56-861b-8f1241627eb1`. Its visible
answer honestly states that the collaborator never inspected or tested the
work and that no independent review exists. Its description of an "inactive"
grant is inaccurate: the underlying refusal message combines several distinct
causes. Improve that diagnosis alongside the permission path. No further setup
was attempted, no new Actor was created, and no model Delivery remains active.

The resumed request used eight responses and 63,167 tokens; the failure follow-up
used two responses and 11,876 tokens. The reviewer used no model responses.
The resumed phase therefore used 75,043 measured tokens and did not complete
the outcome. Together with the stopped attempt, measured outcome usage is
1,681,099 tokens so far; this is not an efficiency success. Original files and
all 14 saved versions are still unchanged, with no new review Artefact.

Evidence under `C:\Development\_temp\floe-upgrade-proof-20260904`:
`collaboration-0.1.46-authority-refusal.json`,
`collaboration-resumed-0.1.46.png`, and
`collaboration-authority-refusal-0.1.46.png`.
The HTTP/WebSocket regression proves creating a binding after Bridge startup;
the installed continuation proves retained reviewers attach and receive the
request. Successful live independent collaboration, including creating and
authorising a new collaborator without restart, remains pending under F33.

### F33 permission delegation and installed upgrade correction — 2026-09-05

The shared operation surface now exposes `capability.grant.list`,
`capability.grant.delegate` and `capability.grant.revoke`. Delegation requires
explicit current permission and a source grant pinned by the caller's session.
It creates a new grant for the recipient Actor; operations, targets and expiry
cannot expand beyond the source. Both the source and delegation permission
remain live dependencies, including through further delegation and Workspace
export/restore. Revocation removes derived access. Account-purpose constraints
are retained without exposing credential material. Actor definition publication
now refuses wrong-principal references before runtime preparation and reports
the specific reason.

The local product explicitly assigns its retained default Floe Actor these
three operations when an active original local product grant supports that
responsibility. It preserves instructions, runtime bindings, existing grants
and independently changed configuration. Removed, revoked or expired policy
grants are not silently restored. This policy is installed at the desktop/CLI
entry points; an ordinary Bus does not grant delegation by default.

Source verification passed: 879 Bus tests with one skipped, 18 final focused
delegation tests, nine documentation checks, and Bus/Bridge TypeScript builds.
The authenticated tests cover both interaction modes, real credential-broker
use, source and permission revocation, narrowing, stale session references,
deep delegation ancestry, and actual Workspace export/restore. The desktop
package passed its Command and Extension host checks.

The first installed package, 0.1.47, failed startup with
`no such table: capability_grant_delegations`. Its schema version was still 12,
so an existing installation correctly skipped migration and lacked the new
table. New-database tests did not expose this. The verified pre-install recovery
contains 1,340 Events at
`C:\Users\jfenech\.floe\recovery\before-0.1.47-20260905`.
No review was submitted to the failed service.

A retained-schema-12 regression reproduces that exact failure. Schema 13 now
triggers the existing atomic upgrade and verified recovery-copy mechanism.
The regression preserves existing grants, verifies the prior database copy,
and checks foreign keys. All 36 upgrade, permission and portability checks pass.
Package 0.1.48 installed successfully. Desktop PID 54556 owns substrate PID 99416
on port 5377. Installed service SHA-256:
`1f8798cf4a76dc2e948d9aedbd20a3a93f60367a6761df78430bd8239708219b`.
The recovery directory is
`C:\Users\jfenech\.floe\recovery\before-0.1.48-20260905`.
The schema 12-to-13 automatic backup is retained under `.floe/bus/backups/`.
Actual integrity and foreign-key checks pass; all 1,340 prior Events, existing
grant rows, account files, host configuration, Floe instructions and runtime
bindings are preserved. The default Floe Actor received exactly the three
delegation operations through its explicit local product policy. Original game
and scene files and all 14 saved versions remain unchanged. Evidence:
`delegation-upgrade-0.1.48.json` in the external proof directory.

The actual browser reconnected without pairing or sign-in, opened the same
review conversation and showed the existing ChatGPT / GPT-5.6 Sol / high-effort
selection as Ready. A continuation asks Floe to discover the supported access
capability, repair one existing collaborator and return the independent review
as a named saved result. No collaborator access or review output was manually
created by the development agent.

### Installed independent review completed — 0.1.48

Continuation Event `evt_a4978ab2-c085-4eae-ad1f-d3de290ac570` began at
05:47:02.604 UTC in the same review Context. Floe discovered the new operations,
created separate grants for `three-seeds-first-player-reviewer`, and published
its repaired definition `actor_definition_912a7582-a941-4ea9-9d27-86307767c290`.
The delegated workspace grant contains only `artefact.inspect`,
`artefact.search` and `workspace.inspect`; the account grant contains only
`credential.use` for the existing account/SecretRef. Both retain their source
and delegation-permission dependencies. No new Actor or runtime binding was
needed. Runtime preparation accepted the reviewer's own authority.

The reviewer completed Delivery `del_d189fd6f-e324-4c86-9ac2-7a5742e4a791`
and returned Event `evt_1b6b16af-70f5-4a92-bf3d-fbb80d97bd96`. It inspected the
exact saved game and brief and their sources, ran a read-only deterministic
game simulation, and ran the existing model and bundle tests. It identified
one concrete improvement: make Rhythmbell's approximately 1.8-second switching
rhythm visible during play. The review distinguished these checks from canvas,
audio, touch and browser layout, which it did not test. Its statement that no
browser was available is only its own discovery outcome: checking browser names
on PATH does not establish that no browser is installed on the host.

Floe resumed automatically on the returned result, independently checked the
model behaviour, and explicitly described a numeric difference between the two
simulation step sizes instead of silently combining their percentages. It
retained **Three Seeds — Independent First-Player Review** as version
`artefact_version_9c6ffd53-8aad-499b-830a-f4753d7e11e2` of
`artefact_591e9c02-59eb-406b-acec-5d2dc87afbd2`. The 3,670-byte Markdown content
has digest `480bc0c0621d68d3cd236fa218d3722166d704e4818a3bd6f71a15bc113bbca4`.
Event `evt_8df39dfb-7695-4e37-ac8f-e0cbed7accee` names and attaches that exact
version; the final completion Event is
`evt_55c6a709-4955-4e02-b916-23079ea0f6ec` at 05:53:26.869 UTC.

The actual browser's named Open button reveals the retained review with the
independent evidence, Floe's recommendation, limitations and proposed user test.
All three new Deliveries acknowledged; no model Delivery remains active. The
original 11 file hashes and all 14 saved versions are unchanged. Exactly one
new review version was added. Evidence in the external proof directory:
`collaboration-completed-0.1.48.json`, `collaboration-result-0.1.48.json`,
`collaboration-reviewer-running-0.1.48.png`, and
`collaboration-review-open-0.1.48.png`.

This successful continuation took about 6 minutes 24 seconds and 38 model
responses: 536,068 input, 10,842 output, 220,544 cached input and 767,454 total
tokens. Reported reasoning is 5,112, already included in output; all three turns
have observed completion coverage. Setup used 245,383 tokens, the independent
review 215,581, and Floe's verification/saved handoff 306,490. Including the
earlier failed attempts, the complete review outcome cost 2,448,553 measured
tokens. Earlier reasoning coverage remains incomplete. This is correctness and
collaboration progress, not an efficiency success or a matched efficiency
comparison. The development agent supplied two repair continuations across the
failed attempts, but did not author the reviewer, its access grants or its work.

F33's observed wrong-principal barrier is closed for this existing collaborator.
F31 remains open: diagnose the measured setup, discovery, repeated source reads,
Windows command recovery and saved-result work before changing prompts or
adding capability shortcuts. Preserve the named result as the quality baseline.
The next independent collaboration proof must also create and authorise a fresh
collaborator after startup, obtain its result, and exercise stopping delegated
work. The full end-state gates and original 18-step campaign remain Pending.

### F31 diagnosis from the completed review — 2026-09-05

The preceding goal turn made concrete progress: installed 0.1.48 repaired and
completed the independent review with exact saved output and preservation
evidence. The next turn verified the same installed parent/child processes and
the retained result before diagnosing the 767,454-token continuation. No model
Delivery was active and no external blocker prevented further testing.

**F34 — Saved text has no continuation after truncation.** `read_artefact`
returned only the first 16,000 characters of the 25,160-byte saved game, with no
range or continuation parameter. The reviewer then read workspace sources in
addition to the saved inputs. The generic need is complete, bounded access to
the exact saved text. The shared runtime tool now supports explicit text offsets
and limits, retains a whole-content digest, and reports the next offset and
first line. Unicode pairs stay intact. Every page repeats authenticated content
retrieval; no content cache bypasses current authority. The change is runtime
representation over the existing exact-content contract, not a new primitive.

**F35 — Returned failure is confused with successful execution.** A nonzero
workspace command sets its tool activity to failed and returns `details.ok:
false`, but the adapter overwrote this with Pi's `isError: false`, which only
meant execution had not thrown. Telemetry and hooks then reported success.
The client recognised some failure text from the command summary but still
assigned a completed status. Source changes now preserve explicit tool failure
in telemetry, hooks and the work log. The client uses the same failure for its
text and status, including retained earlier command evidence. Successful exits
say “Completed a workspace step”; they do not claim the work was verified.
Failure text no longer asserts that Floe is adapting without that evidence.

**F36 — Windows command quoting changes the submitted program.** A direct
read-only reproduction through `createBashTool` showed that the valid command
`node -e "const label = 'saved work'; console.log(label)"` failed with an
unterminated-string error. This explains the reviewer's quoted-command recovery
attempts. The Windows launcher now uses cmd.exe's `/d /s /c` form and verbatim
command arguments, matching the installed Node runtime's own shell launch.
The existing hidden-window, environment filtering, output bounds and nonblocking
process handling remain in use. Regressions cover quoted programs, executable
paths with spaces, filenames with spaces and shell characters, nonzero exits,
output capture and event-loop availability. The quoted-command regressions
failed before the correction and now pass.

Focused verification passed 77 shared runtime/adapter tests, 26 conversation
tests and all 12 workspace-command tests. Runtime TypeScript checking passes.
The full runtime suite passed all 389 tests, and nine documentation checks pass.
The installed repeat is still pending; these source changes do not yet establish
an efficiency improvement. The full acceptance scope remains intact.

Installed 0.1.49 after the packaged Command/Extension checks and desktop build
passed. Recovery contains 1,347 Events at
`C:\Users\jfenech\.floe\recovery\before-0.1.49-20260905`.
Service SHA-256 is
`5300ebc17d2d4a5d9a3bd2926e286bc5aa3f89fdb4364bccbf21fbb640fc343b`.
Desktop PID 31656 owns substrate PID 54632 on 5377. Schema remains 13.
Integrity and foreign keys pass; existing Events, grants, account files, host
configuration, Floe definition and runtime bindings are unchanged. No extra
policy grant was added. Browser reload reconnected without pairing or sign-in.

Fresh-collaboration baseline `fresh-collaboration-before-0.1.49.json` records
18 workspace files and 15 saved versions. The earlier F20 probes include one
false all-zero digest and two pre-retention references to a mutable brief path.
Those three existing mismatches are explicitly distinguished from new changes;
their history is not rewritten or counted as proof of readable saved content.
The correctly retained original content for the two pre-retention references
still exists under its digest. The current saved game, brief, scene and review
all retain their verified content. Evidence helper
`floe-fresh-collaboration-proof-20260905.mjs` observes current Deliveries,
bounded tool metadata, usage, failure status and preservation without creating
Actors, granting access or manufacturing results.

Through **New with Floe**, the next actual request asks for a fresh independent
reviewer, its own assessment without consulting the earlier review, evidence
from the current saved game and brief, and a named saved handoff. Existing work
and earlier saved versions must be preserved, with no external publication.
The intended live sequence is fresh Actor creation/authority/attachment,
stopping its active delegated response, restart durability, and deliberate
continuation. It remains pending until those actual controls and results pass.

The exact original 18-step scenario has also been restored verbatim to
`end-state-verification.md` from the operator's original handoff; every step
remains Pending. Neither this scene nor the game closes that full campaign.

### F23 complete response accounting and bounded discovery — 2026-09-05

The previous goal turn made concrete progress: installed 0.1.36 completed the
precise edit/review attempt and preserved its earlier saved version. No external
blocker remains for local proving.

The Pi adapter now records usage at every assistant `message_end`, including
tool-producing responses, and deduplicates the same message seen again at
`agent_end`. A coverage record gives the observed response count. Completed
responses remain recorded if a later step is interrupted; absent provider usage
stays null. Earlier final-response-only records cannot be used as full totals.
Regression tests cover multiple responses, interruption and missing usage.

Capability search now presents summaries; selecting an exact operation loads its
input contract. Full result schemas remain available explicitly for integration
work. The Bus still supplies the definitions and evaluates current authority.
Using the real operator catalogue, `artefact` search plus one publication input
contract used 5,835 characters instead of 27,056. The broader `artefact version
publish` query used 10,459 instead of 105,926. These are presentation-size
measurements, not measured token savings. Evidence:
`C:\Development\_temp\floe-upgrade-proof-20260904\discovery-size-proof.json`.

Installed 0.1.37 through its generated installer with a verified 1,307-Event
recovery copy. Service SHA-256:
`202842A291DE9A9F103A924F664A9FA66B21569BE36FCBB530F0D86A52D8AFE4`.
The real Rhythmbell colour correction completed in about 102 seconds, using
20 tools and 18 recorded model responses. Reported totals: 205,711 input tokens,
2,112 output tokens, 32,512 cached input tokens, zero cache-write tokens, and
240,335 total tokens. Reported reasoning was 864; it is not added again to the
total. Every response has usage, indices 1–18 are unique, and coverage agrees.
This is complete reported usage for this attempt, not proof of actual billing.

Request `evt_b263a2f2-e9bc-4433-9990-4bc0625318fe`, Delivery
`del_e9a9bb19-dd9c-4081-9437-c0cc19556f10`, RuntimeTurn
`rt_2600a8ad-acdb-4fc7-8b2a-0c7a1e640d82` produced version
`artefact_version_5a6ec787-af07-40e1-b2d4-e6fe05b5a402` (ordinal 6), attached
to reply `evt_db7a5904-2653-41be-a4b0-9922380fec19`. The only working-file change
is `a violet bellflower` becoming `a sea-green bellflower`. The browser opened
the result; its previous retained bytes remain intact. Complete evidence:
`C:\Development\_temp\floe-upgrade-proof-20260904\usage-run-0.1.37.json`.

This remains expensive. The trace showed one failed discovery using the display
`artefact.search@1` as an operation ID. Display now labels ID and version
separately, and query guidance asks for short keywords. It also showed a shell
command used solely to calculate file checksum and byte count. Workspace read
and edit now return that exact byte identity with their result. BOM, UTF-8 and
CRLF checks verify the identity is of actual whole-file bytes, even when the read
displays only a line range. Publication still verifies the snapshot before saving.

The 0.1.37 Bridge suite passed 371 tests in 24 files. The subsequent file identity
and discovery checks passed 78 tests; Bridge type checking and nine documentation
checks passed. The 0.1.38 installer and measured repeat completed:

- Installed service SHA-256: `623C5003A69CF5A9B448E36B6DA6AD78844FBD93A45BE8F0F3D8FBF1AAA30CC5`.
- Verified recovery copy: `C:\Users\jfenech\.floe\recovery\before-0.1.38-20260905` (1,310 retained Events).
- Request `evt_5b8d1fc5-e236-4e52-a34a-f08228e5c499`, Delivery `del_7be95d62-bdcc-4ca6-9438-503041194826`, RuntimeTurn `rt_7569bd3d-87ca-49ce-8b87-0041cc2c1373`.
- Exact edit: `a round golden flower` → `a round ivory flower`; all other bytes equal retained version 6.
- Version 7: `artefact_version_142a8b4f-6d53-47d7-9761-f933fff41015`, digest `c826a4ef8ae3e43c7fea28572fd44172a7fe1d9c5dffac82a4ff0efd300a1880`. Attached by Event `evt_f18f616d-f75f-4be3-8ce1-85a38a9eec7b` and opened in the real browser.
- All 12 model responses have usage records, exact indices 1–12 and terminal coverage. Reported totals: input 99,478; output 1,203; cache read 34,048; cache write 0; total 134,729. Reasoning 372 is already included, not additive.
- 11 tool calls, about 69 seconds. Compared with the preceding equivalent edit: 43.94% fewer total tokens and 45% fewer tools. This is one observed pair, not a controlled benchmark or a general performance guarantee.
- Evidence: `C:\Development\_temp\floe-upgrade-proof-20260904\usage-run-0.1.38.json`.

Efficiency remains an acceptance concern: 134,729 tokens for a small edit is
still costly, and the model requested two optional full result schemas.

### F18 direct-response cancellation increment — 2026-09-05

Observed need: stop the active direct response from its conversation, retain
what already happened, and deliberately continue. The client chooses the exact
visible response. A shared `runtime.delivery.cancel` operation cancels its
existing transport reservation and bundled queue obligations, atomically revokes
runtime operation authority, retains a cancellation message, and pushes abort to
the Bridge. It introduces no Context lifecycle or execution ledger.

This operation is explicitly one response, not a promise to cancel separately
delegated work. Canonical Scope executions use `scope.execution.stop`; the direct
operation refuses them. Full connected-work cancellation remains a separate
end-state proof obligation.

The Bus rejects new completions after cancellation and preserves a result that
already committed. Pi error/aborted completion cannot turn partial text into
success. The Bridge retains a pushed or claimed next reservation while the
previous response unwinds, with duplicate active pushes ignored; no polling is
introduced. The UI preserves the exact Stop request across uncertain transport
and shows failure without falsely clearing active work.

Focused tests: Bus cancellation/turn results/Scope backend 21 passed; conversation
and Stop client 27 passed; Bridge adapter/daemon 95 passed, with the final 27
daemon tests rerun after the duplicate-push guard. Bus and Bridge TypeScript and
the packaged client build passed. Final NSIS build:
`C:\Development\_temp\floe-upgrade-proof-20260904\build-0.1.39-final.log`.

Installed proof completed using the same real Floe home and test Workspace:

- Version 0.1.39, service SHA-256 `9DAD0E4200217AC7A527E713F031AE40E59F0D2D53C32ACF2C841F5859EAF1DD`. Verified recovery copy `C:\Users\jfenech\.floe\recovery\before-0.1.39-20260905` retains 1,313 Events, previous installation, configuration and accounts.
- Real request `evt_887c78a3-c8b4-46f5-a885-d9da183d1ba4` asked for a twenty-case playtest plan. Delivery `del_2112450b-1fbe-466f-ab4a-6488d70b8a68` entered Pi, inspected the Workspace and read the brief.
- Browser Stop invoked canonical receipt `opinv_12552ee32cfcca6f7cca733c3a62a433`. Cancellation Event `evt_f483a0f9-2953-4ae9-ae3c-49551aea27ef` and authority revocation committed at `2026-09-05T01:25:52.437Z`. The last runtime telemetry arrived 65 ms later. The queue and reservation remain cancelled, with no natural completion.
- The browser showed Stop beside the working indicator, then the retained “Floe status” message and an enabled composer. Screenshots and accessibility observations verified this interaction. The brief digest stayed equal to version 7; no playtest file was created.
- Floe was restarted after checking that no other active deliveries would be interrupted. Database integrity passed. A fresh browser load restored the cancellation message, preserved the connected model, and did not resume the cancelled response.
- A deliberate new request `evt_9032daaf-aedc-4846-9860-0ca3fcdaf156` asked for the current plant colours without resuming the plan. Delivery `del_d92fcc1f-5b05-4205-88a2-358112b58b08` completed through the real model in about 17 seconds (four responses, three read-only tools). Result `evt_a2bd9262-6fd9-4557-a63d-77b052c19b03` correctly named ivory, copper-orange and sea-green and appeared in the same conversation. The brief remained unchanged and no playtest file appeared.
- Evidence: `stop-stopped-0.1.39.json`, `stop-restarted-0.1.39.json`, and `stop-continued-0.1.39.json` under `C:\Development\_temp\floe-upgrade-proof-20260904`.

The continuation reported 19,727 total tokens across all four responses
(19,591 input, 136 output, no reported cache use).

This proves one active direct response, restart durability and deliberate
continuation. It does not establish delegated-work cancellation, in-flight
external-effect reconciliation, mobile operation, native-window UX, or full
end-state completion.

### F9 permissions corrected; F10 readiness corrected — 2026-09-04

**F9 — New Workspace permissions.** The desktop and CLI now explicitly install
the local product's starting policy through the existing import-policy seam.
Only the declared `floe` Actor in a newly created, initialization-authorized
Workspace receives the closed capability set. Tests cover equivalent behaviour
with different backing, no implicit authority for other Actors or ordinary
server instances, and refusal to restore explicitly revoked grants on policy
renewal. The reusable substrate does not supply this product default.

The packaged proof substrate was restarted using the same home and Workspace.
Actual browser reload entered the Workspace without pairing. The missing
Workspace permissions message disappeared. No Workspace identity was relabelled
and no test-created permission was injected into this live attempt.

**F10 — A saved model could not receive work.** The model-selection integration
test reproduced a resolved canonical binding with an Endpoint still marked
`runtime_unconfigured`. Binding changes now push configuration readiness after
the enclosing synchronous transaction settles. Queued work starts with exact
Actor/runtime pins. Attachment derives configuration readiness from the current
binding instead of replaying an old import receipt's status. Regression checks
cover queued work, unchanged active Delivery pins, rollback, reattachment,
credential revocation, and preservation of error, retired, offline and waiting
states. A real provider conversation is still required to complete this proof.

The setup screen also called Floe “ready” while account setup was incomplete.
Its service status now says “Floe is running”; conversation readiness remains
in the model controls. Browser inspection confirmed the corrected wording.

Verification: full Bus suite passed, 114 files and 836 tests, one skipped;
model-selection integration passed both tests; App and Bus builds, nine
documentation checks, and packaged Command and Extension host checks passed.
The proof substrate is PID 101500 on port
63068. The installed user substrate was not restarted or replaced. These are
regression and onboarding results, not acceptance of the full product campaign.

The previous ChatGPT sign-in expired without completion. A new browser sign-in
was opened at approximately 09:05 UTC using the catalog's GPT-5.6 Sol selection.
The app is waiting at “Open ChatGPT sign-in”. Account interaction remains the
next required external step; no completed sign-in or real model turn is claimed.

At 09:06 UTC, both the proof substrate and its OAuth callback listener were
verified running under PID 101500. Opening the actual provider authorization
link in a visible in-app browser tab reached `auth.openai.com/log-in`, which
requires the operator's account login. The operator was asked to complete that
page. Do not fetch another application's private credentials or manufacture a
provider result to pass this step. Leave the same proof process and Workspace
available. After sign-in, inspect the Floe page, verify the saved model and
readiness, and submit the first bounded outcome through its conversation form.
If sign-in expires, begin a fresh attempt through the same supported UI.

### Current F8 evidence and next F9 barrier — 2026-09-04

The local browser now lists subscription providers and reaches the provider's
actual sign-in flow. The ChatGPT attempt reached the provider method choice,
browser authorization link and optional return-code question. Account interaction
was requested from the operator; no completed real sign-in or model turn has
been verified. Frontend edits restarted the pending form; start a fresh sign-in
after the current packaged build is resumed. Do not reuse an expired link.

The local sign-in adapter invokes canonical account operations and the protected
credential ingress. Tests cover cookie-bound prompts, cancellation, forwarded
request refusal, safe failures, Windows DPAPI storage and absence of reusable
credentials in responses and receipts. Prompt questions and provider links stay
visible when progress messages arrive. Native account disconnection still uses
its declared confirmation boundary; the browser does not bypass it.

Conversation model selection now composes canonical Actor, Runtime Profile and
binding operations in both clients. It resolves the exact Endpoint binding,
retains unrelated Actor instructions and grants, refuses stale saves and reads
the saved selection after reload. A successful account connection refreshes the
binding before selection because credential resolution may itself replace that
binding. Host-owned, narrowly scoped runtime-account grant/revoke operations
support the selection without exposing host authority to the browser.

An integration attempt exposed a remaining old resource comparison in runtime
credential access: imported provider-account grants were checked as if they
still targeted individual Runtime Profiles. Execution and readiness now use the
SecretRef's exact resource and the active Actor grant. The regression passes
through a real protected credential read and proves revocation prevents reuse.
This test uses a fake provider exchange; it is not a real model result.

**F9 — Fresh Workspace Actor authority is missing.** Actual browser use now shows
“This collaborator needs workspace permissions before it can start.” The import
policy in `store.ts` authorizes only `legacy_retained` Workspaces. New Workspaces
receive `operation_authority_unmapped`; no supported product path currently
assigns their default Floe Actor its normal capabilities. Do not relabel a new
Workspace as legacy or remove the refusal. Complete an explicit local product
policy through the shared grant mechanisms, then repeat first-conversation setup.

Import ownership is guarded: independently changed canonical state causes a
conflict for a changed import, while an identical prior inventory replays its
receipt. It is not correct to claim the importer always overwrites a selected
model. Reattachment after canonical model selection, policy renewal and changed
Workspace files still need live verification.

Current checks: the full app suite passed (40 files, 211 tests, two existing
TODOs), as did 31 focused Bus tests and nine documentation checks. App and Bus
builds passed with the final changes. The refreshed package passed its isolated
Command and Extension host checks. It is running against the same proof home
on port 63068 as PID 77264. Browser reload again returned directly to setup
without pairing; the permission barrier remained visible. The installed user
substrate remains PID 42248 on port 5377. No new installer has been deployed.

The provider catalog currently offers GPT-5.6 Sol but does not list Astra. The
browser proof selected Sol from the actual catalog; no unsupported model ID was
invented. Provider catalog freshness remains a follow-up for the requested
model capability. The complete acceptance campaign remains pending.

- **A0 — Start as an operator.** Inspect the handover and running processes. Use an isolated test
  workspace and a known build without clearing existing credentials, configuration, or history.
  Exercise onboarding, provider/model selection, opening a workspace, and the first conversation.
  Record the actual build, model, instructions, environment, and first visible result.
- **A1 — Complete and correct useful work.** Begin with one bounded outcome, then exercise the
  scene example: a concept, a small set of independently inspectable objects, a correction to one
  object, and an assembled result. Let Floe discover and form the required organisation. Verify exact
  output versions, preservation of unaffected work, and useful results in the app.
- **A2 — Inspect and intervene.** Use the normal work surface to understand progress, respond to a
  decision, follow a reference, correct the work, and stop it. Restart the relevant runtime and
  confirm that active work recovers correctly and stopped work stays stopped. Resolve an encountered
  failure through the product's recovery path and verify that history remains understandable.
- **A3 — Check a different organisation.** Attempt an open-ended investigation where the next step
  depends on findings. Observe whether actors can collaborate without a predefined pipeline or a
  new substrate mechanism. Use that evidence to assess generality.

At each failed step, record expected and observed behaviour, diagnose the smallest correction at
the appropriate layer, implement it, and repeat that same step through the app. Do not pre-create
the test's actors, results, or routing through developer tools and call that an operator success.

## Evidence and efficiency

Keep screenshots or visible UI observations with the relevant Context, execution, result, and
receipt references. Record what the app actually communicated, not only what internal state proves.
Include friction that prevents understanding or correction even if the underlying operation succeeds.

Use existing usage telemetry first. Record available input, output, cached and reasoning tokens,
elapsed time, failed/repeated attempts, and human interventions across the complete outcome.
Keep unavailable measurements explicit. Compare instruction or context changes against the same
outcome and quality criteria. Do not replace the requested capable model with a cheaper model merely
to improve token or cost totals.

The current computer-use surface exposes a browser, not native Windows controls. Browser testing
cannot establish that installer, native confirmation, credential, or desktop-only interactions work.
Keep those checks open until an actual supported route can exercise them. Never bypass authentication,
replace the native broker with a privileged test proxy, or count mocked UI as product evidence.

## Completion evidence

Record a pass, observed failure, or untested state for every attempted experience. The milestone is
complete only when its expected work and controls succeed through the actual app, relevant regression
checks pass, affected documentation is accurate, and the operator need not repair internal machinery.
An intermediate checkpoint must name the next unresolved experience and preserve the full end-state
acceptance scope.

