# Approval feedback proving attempt — 7 September 2026

The real browser repeated the local preview request against installed 0.1.74.
The operator needs to inspect an exact action, request changes, and retain a
recognisable decision beside the work. Preparing that request does not authorise
the copy or any internet publication.

## F78 — supporting evidence changed the action being approved

The actual export preflight created pending request
`approval_request_bb65a3a2-cafc-417a-91d1-a3647cf22492` for the exact saved gallery
and `published/night-market/index.html`. Policy stopped execution before a file
was created. Floe cloned that request as
`approval_request_cc188ef3-d824-4a1b-891f-34e7948fea67`, adding the test and build
versions to the bound action and rewriting its expected effect.

The retained actions differ. The exact-action verifier rejects changed action
digests, so the second request cannot authorise the original export. This was
diagnosed from the records and verifier; the invalid request was not approved
to exercise that refusal. The browser instead recorded **Changes requested** at
03:56:33 UTC, with the correction to preserve the original action and attach
supporting reports in the conversation. The original request remains pending.

The smallest correction belongs in Actor instructions: reuse an operation's
approval request unchanged; presentation belongs in the accompanying message.
The paragraph is in both default instruction sources and was published to the
existing campaign Floe Actor through normal operations. Astra made this change;
Floe's grants and runtime binding were preserved. The fresh model repeat below
now proves that it reuses the original request and carries extra evidence in the
conversation, without cloning or changing the action.

The completed attempt used 21 responses and 434,355 reported tokens: 340,877
input, 4,518 output and 88,960 cache-read tokens. This is an unsuccessful approval
preparation attempt, not efficiency proof. It reused the saved review and three
exact versions without another review, rebuild, image inspection or local copy.

## F79 — saved correction was hidden and labelled as rejection

The existing request retained `decision: changes_requested`, its reason, and a
public Context Event. The client displayed its aggregate terminal status as
**Rejected**, hid the reason in collapsed details, and excluded the decision
Event from conversation history.

The client now shows the exact decision and reason, includes canonical approval
decisions in bounded Context history and live updates, and offers **Review
decision** through the existing Actions surface. Ordinary message claims do not
gain decision controls. Invalidated or cancelled status is not hidden by an old
decision. Evidence labels come only from explicit attachments to those same
versions in loaded Context history; no approval action or identity is rewritten.

The compiled browser proves the saved correction survives reload, opens the same
request, shows **Changes requested** and its reason, and opens the named exact
test report. The existing report shows 22 checks and its original gallery hash.
No core routing, new record type, or polling was added.

Forty-eight focused client tests and the application build pass. Both 0.1.75
installers completed successfully. The installed desktop, broker and service
match their build outputs. Recovery is saved under
`C:/Users/jfenech/.floe/recovery/before-0.1.75-20260907-approval-feedback`.
Post-install comparison preserves all 1,577 previous Events, 36 Actors, 31 saved
versions, 11 Scope executions, both approval requests, accounts, configuration,
grants and runtime bindings. Database integrity and foreign keys pass at schema
13. The local export destination remains absent.

Installed desktop checks pass all nine Workspace live connections and saved
conversation reads in all eight Workspaces with history. Local service/model
health is connected; cross-Workspace access remains refused. The browser
recovered automatically and reports **Floe is running**. Native window
interaction is still unproven.

## F80 — the actual approval cannot resume the requested work

The standalone decision did not create a Delivery back to Floe. The earlier
outcome asked only to prepare approval; it did not configure a continuation.
Existing Scope decision bindings and output routing already provide continuation
mechanisms, while this preflight request has no decision binding. That observation
does not justify a new substrate mechanism or waking every Actor by default.

At 04:27:19 UTC the actual browser asked Floe to arrange the local preview so the
operator's decision leads to the appropriate result without another message,
using existing capabilities and preserving the completed review and exact
evidence. Request `evt_dab3dabc-f9c4-4af9-a3c0-1b9163cc1cb3` ran as
Delivery `del_60297def-7e2f-4020-a60e-cb049aa51363`. It acknowledged at 04:29 UTC
without a follow-up or approval during the attempt. Floe inspected the original
request, preserved it, and returned the three existing versions with explicit
names. The browser displays the corrected handoff. All eight prior review runs,
three versions, two requests and four Actor heads are unchanged; no file was
exported.

Floe accurately reported that the original request has no decision binding and
the available contract cannot attach one later. Its claim that replacement was
prohibited refers to Astra's earlier test correction, not a permanent operator
requirement. Removing that test constraint alone would not connect the original
governed invocation: governance retains its own request set and always creates
preflight requests with `decision_binding: null`. A manually cloned request is
not a substitute for the invocation's retained request.

This repeat cost 496,845 reported tokens across 15 responses, with five history
reads and twenty discovery calls. It diagnosed an obstacle; it did not achieve
automatic continuation or establish an efficiency improvement. The emitted
handoff and final response also repeat the same information.

The next bounded change must make an explicitly requested continuation use the
existing Event/Delivery or Scope routing mechanisms for the actual governed
request. Inspect those contracts before selecting the change. Do not restore
legacy Context subscriptions, infer a UI-owned callback, require a pipeline for
ordinary conversation, or add recurring status checks. The current handoff also
names the pending request as text instead of offering a direct conversation
action; meaningful pending-request navigation remains part of this proving slice.

Native window interaction, automatic continuation, successful approved export,
and the full end-state gates remain unproven. No full gate closes
from this bounded correction.

Evidence: `C:/Development/_temp/floe-approval-feedback-evidence-20260907/` and
the preceding `C:/Development/_temp/floe-review-decision-evidence-20260907/`.
